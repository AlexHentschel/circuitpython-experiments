#pragma once

// Copy this file to `include/secrets.h` and fill in your WiFi credentials.
// This project ignores `include/secrets.h` via .gitignore.

#define WIFI_SSID     "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
