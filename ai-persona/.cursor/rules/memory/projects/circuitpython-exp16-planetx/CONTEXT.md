# Context — circuitpython-exp16-planetx

**Family**: `circuitpython` · **Repo**: `/Users/alex/Development/VsCode/CircuitPython/2026-09_Exp16_BPI-Bit-S2_CircuitPy_PlanetX/` · **Goal note**: `Notes/overall_goal.md`

**Status digest** — *live/chronological state is in § Resumption point below (maintained per session); this header holds only the stable anchors + current headline. The per-stage confirmation blow-by-blow (commits `b99152b`/`94e54ca`/… , frozen `code_stageN.py` siblings) lives in § Resumption point Sessions 19/20/22/33 and `SESSION_LOG.md`.*

- **Headline**: first-milestone scope (async 5×5 display + async buttons, PoC-quality with a library upgrade path) is **discharged end-to-end on real hardware** — **K1/K2/K3 all evidence-supported**, all four original test stages confirmed on-device (Session 33). Stages 0/1/2 frozen as `code_stage{0,1,2}.py`; Stage 3 in `code_stage3.py`.
- **Open threads**: (a) font-spacing **Phase 4 landed 2026-09-20** (`show_string` → `SpacedGlyphColumnFeeder`); **Phase 5 = Alex's on-device re-confirm**. (b) `code_stage2.py` steps 11/12 (rotation *during* an in-flight Tier-2 animation) drafted 2026-09-13, **not yet run on-device** (step 11 duration formula assumes fixed `WIDTH`/glyph — stale vs spaced layout). (c) `planetx`-package Stage-3 on-device re-run pending (Session 39+).
- **Board**: UID `0740D10F1BE9`, CircuitPython **10.3.0**. **Brightness floor (authoritative, Alex)**: 0.01 off, 0.02 lowest lit; library default `core.py BRIGHTNESS = 0.20`.

## Scope & goal

Prove a CircuitPython stack on the **BPI-Bit-S2** (ESP32-S2, micro:bit form factor, onboard 5×5 WS2812) can run the **LightTower challenge** as a PoC. Stack to build; LightTower is the demonstration, not production firmware. Library-level code should be upgradable to production with moderate effort.

**First major milestone (interim):**
1. Async LED-matrix library — fork Exp14 `lib/display/` onto 5×5 (brightness cap 20%). **Hardware scope (locked):** square WS2812 / NeoPixel only, max 8 on a side; realistic sizes **5×5** and **8×8**. Charlieplexed matrices out of scope. **5×5 ↔ 8×8 via file replacement and/or localized edits**: geometry/LUT, icons, arrows, font. Not a second library. Exp14: arrows live in `icons.py` beside icons; font is `font_*/` + `_FONT_PATH` in `core.py`.
2. Async button library — PlanetX C/D (+ board A/B later for LightTower).

**Portability (design goal, not a Hardware Abstraction Layer [HAL]):** student-facing LightTower **operations** should survive a later RP2350+8×8 switch. Constructor parameters and config may change in student code and in the library. Spec: exp16 `Notes/student-api-portability.md`. Realistic for display+buttons now; motor/light later as semantic APIs.

**End demonstration:** `/Users/alex/Development/Isana/LightTower-challenge` requirements (`2026-05-15_lighthouse-keeper_requirements_v1.0.md`). Needs more than LED+buttons (servo sweep, light sensor) — later.

## Constraints (Alex, 2026-09-03)

- Experiments human-conducted; agent writes software. Overnight independent iteration is the aim; physical reset of a stuck board is acceptable.
- Host Python: `/Users/alex/Development/PythonVEs/CircuitPython_3.13_VsCode` (Mac/Cursor; pytest).
- Focus: **asynchronous** programming.
- Do not change Alex's working setup without explicit permission. Workspace-level CircuitPythonSync currently still points at Exp14's YD-RP2040 / 10.1.3 — override per-experiment, do not rewrite the shared workspace settings without asking. **Which file is "the shared workspace settings" — corrected 2026-09-13**: the actually-active multi-root workspace for this Cursor session is `~/Development/Cursor Workspaces/circuitpython.code-workspace`, **not** the in-repo `CircuitPy_VSCode.code-workspace` (a separate, similarly-populated-but-not-identical file) — verified via `workspaceStorage`, not assumed by location. See `../../concepts/tooling.md` § *Determining which `.code-workspace` file actually backs a Cursor session*. Re-verify per session if this matters again.
- **Destructive ops hard gate** (persona-wide, Exp16 reminder): never delete/overwrite/rewrite-history without a ledger grant. Protocol: `/Users/alex/Git/rnd-ai-skills/generalized-agent-learnings/destructive-operations.md`. Ledger: `memory/PERMITTED_DESTRUCTIVE_ACTIONS.md`. Always-on stub: `06-destructive-operations.mdc`. Local reminder banners: exp16 `ai-notes/NOTES.md` + `INDEX.md` (gitignored working store).
- **Standing §4 park (confirmed 2026-09-04):** `…/2026-09_Exp16_…/ai-notes/_parked/` — move here instead of delete. Policy `_parked/README.md`; index `_parked/MANIFEST.md`. Deleting parked copies still gated. Exp16 only.
- Working notes: local gitignored `ai-notes/` in the exp16 folder (untracked 2026-09-08). Durable claims: `Notes/` + this persona folder. Chat stays high-level.
- Coarse git commits on working branches when a chunk is done or enters revision. History of *reasoning* → `ai-notes/`, not git messages alone.

## Entry points (links, not copies)

| What | Path |
|------|------|
| Project README (goal, hardware, tests, deploy) | `…/2026-09_Exp16_…/README.md` |
| Goal / working prefs | `…/2026-09_Exp16_…/Notes/overall_goal.md` |
| Goldfinger pinout (CC BY-SA, unmodified) | `…/Notes/bpi_bit_v2_goldfinger.jpg` + `bpi_bit_v2_goldfinger.jpg.license` — silk / marketing; electrical schematic is the next row |
| BPI-Bit-S2 schematic (vendor names **BIT-Lite** / **BIT-V2** / wiki **Bit_Lite**) | Official BananaPi Bit-S2 docs link [BPI-BIT-Lite-Doc `sch/BPI-BIT-Lite-V0.2.pdf`](https://github.com/BPI-STEAM/BPI-BIT-Lite-Doc/blob/main/sch/BPI-BIT-Lite-V0.2.pdf) (repo MIT; README is one line). **This generation (ESP32-S2FN4R2), not** original-bit `BPI-BIT-Hardware`. Title block: `ESP32-S2-BIT Lite` / `BPi-Webduino-Bit`; KiCad `BPI-BIT-Lite-ESP32-S2.sch` + `Sensor.sch`; 25× WS2812B, BTN-A/B, LUM, buzzer, USB-C, goldfinger `BIT-P0`…`P20`. File renamed from `BPI-BIT-V2-V0.2.pdf` (commit `775cf99`, 2022-08-02). Pin-level SoT remains CP `pins.c` + goldfinger JPEG until a schematic net is independently decoded. **Local alternate (may vanish, no `source.txt`):** exp16 `ai-notes/BPI-Bit-S2_Hardware/BPI-BIT-Lite-V0.2.pdf` — blob `ea1335e` size **183103** bytes; last checked **2026-09-14** matches `main`. How-to-check: `GET /repos/BPI-STEAM/BPI-BIT-Lite-Doc/contents/sch/BPI-BIT-Lite-V0.2.pdf` size/sha vs local file; if different, notify Alex (do not silently replace). |
| Board interface photo (CC BY-SA, unmodified; README eye-catcher) | `…/Notes/bpi_bit_v2_interface_en.jpg` + `bpi_bit_v2_interface_en.jpg.license` |
| Button brainstorm (ChatGPT, micro:bit v2 then RP2350) | `…/Notes/Button_chat.md` |
| Alternate button sketch | `…/CodingTutor/mini-project-scatches/button-library.md` |
| Display library to copy | `…/2026-04_Exp14_…/lib/display/` (`README.md`, `geometry.py`, `_constants.py`, `core.py`, `icons.py`) — **style + algorithm SoT**; Exp16 works on its copy |
| 5×5 LUT / orientation (formula `index = row + 20 - column * 5`) | `…/2026-02_Exp09_…/lib/display_v0.py`, `lib/microbit.py` |
| Micro:bit 5×5 pictograms (already in Exp09 `Image.*`) | `…/2026-02_Exp09_…/lib/microbit.py` (~line 245+) |
| LightTower requirements | `…/Isana/LightTower-challenge/2026-05-15_lighthouse-keeper_requirements_v1.0.md` |
| Portability spec (student operations across 5×5 → 8×8) | `…/2026-09_Exp16_…/Notes/student-api-portability.md` |
| ElecFreaks PlanetX / Nezha protocol sources (MicroPython + MakeCode; not CPy) | exp16 `README.md` § *ElecFreaks PlanetX / Nezha — protocol sources* — `PlanetX_MicroPython`, `EF_Produce_MicroPython`, `pxt-PlanetX`, `pxt-nezha2`. **Local snapshot (gitignored, may vanish):** next row. |
| BPI-STEAM predecessor (original **bpi:bit** ESP32 / Webduino) — not CPy, not Bit-S2 pin-compatible | **`../../concepts/led-driving.md`** (shared 5×5 index vs GPIO trap); `../../crossref/BY_TOPIC.md` row *BPI-STEAM predecessor*. Repos: [BPI-BIT-Hardware](https://github.com/BPI-STEAM/BPI-BIT-Hardware), [MicroPython-Samples](https://github.com/BPI-STEAM/MicroPython-Samples) (`microbit/display.py`; do not treat `01.leds/heartbeat.py` as matrix), [BPI-BIT-Webduino](https://github.com/BPI-STEAM/BPI-BIT-Webduino) (firmware hub), [webbit_i18n](https://github.com/BPI-STEAM/webbit_i18n) (`blockly/samples/en/bit-s-02.jpg` = 5×5 heartbeat Blockly demo). **Local `MicroPython-Samples`:** next row. |
| Local vendor snapshots (`source.txt`-pinned) | exp16 `ai-notes/Elecfreaks-Repos/<folder>/` — **GitHub remains canonical**; local is an offline alternate. Folder name is a misnomer: one of three trees is BPI-STEAM, not ElecFreaks. **Not present locally:** `pxt-PlanetX`, `pxt-nezha2`, `BPI-BIT-Hardware`, `BPI-BIT-Webduino`, `webbit_i18n`. **Freshness:** before using a local tree, compare `source.txt` SHA to GitHub default-branch HEAD; if they differ, **notify Alex** (do not silently refresh). Expected stale window: months. Last checked **2026-09-14** — all three matched `master`. Pins: `PlanetX_MicroPython` `268740c` (elecfreaks, 2025-06-07); `EF_Produce_MicroPython` `c0b3a53` (elecfreaks, 2026-03-13); `MicroPython-Samples` `c03ed50` (BPI-STEAM, 2019-09-03). How-to-check: `GET /repos/{owner}/{repo}/commits/master` vs the SHA in that folder's `source.txt`. |
| Vendor-vs-Exp16 unpack (display.py, PlanetX modules) | exp16 `ai-notes/vendor-sources/` (gitignored, may vanish) — start `INDEX.md`. Durable one-liners: `concepts/fonts.md` (BananaPi `CharData` ≠ DAL); CONCLUSIONS light-sensor jack constraint. Not a `lib/` port. |
| Nezha V2 motor I2C (durable) | **`../../concepts/nezha.md`** — opcode table + MP-vs-MakeCode disputes. Unpack (gitignored): `ai-notes/digests/nezha-v2-motor-protocol.md` |
| Working notes (local, gitignored) | `…/2026-09_Exp16_…/ai-notes/` (`INDEX.md`, `NOTES.md`, cadence `plan/reflection-cadence.md`) |
| Frozen on-device test milestones (not copied to board) | `…/2026-09_Exp16_…/code_stage0.py` (Stage 0, `b99152b`); live program is `code.py` |
| Stage 3 test script (Tier 2 + onboard + PlanetX pumps; K3 mechanism confirmed 2026-09-13; `planetx` package Session 38, on-device re-run pending) | `…/2026-09_Exp16_…/code_stage3.py` — `from buttons import OnboardButtons` + `from planetx import PlanetXButtonSensor` + `gather` of three tasks |
| Deploy-to-board scripts (human-run; agent never writes to the board) | `…/2026-09_Exp16_…/scripts/sync_files_to_board.py` (manifest files: `.vscode/cpfiles.txt` → board `code.py` etc.) + `…/2026-09_Exp16_…/scripts/sync_lib_to_board.py` (filtered `lib/` sync — excludes `__pycache__`/`.DS_Store`/`README.md`/`*.pyc`, mtime-aware; replaces the extension's unfiltered "CP Copy Libs to Board", see `../../concepts/tooling.md`). Lib script **validated on-device 2026-09-13** (bracketed-repro S1: 19/19 files byte-identical, excludes proven) and committed (`7ae8157`, together with the `README.md § Deploy` script-first recipe). Usage lines live in each script's own docstring. **Runnable via Cursor's Run Task UI, not just a manual terminal** (Session 31, 2026-09-13): `.vscode/tasks.json` has 4 tasks (`Sync Files/Lib to Board` × dry-run/real), all pinned to `${config:python.defaultInterpreterPath}` (not ambient `python3`) — **confirmed working** by Alex. `cpfiles.txt` — never put a comment on the same line as an active `source -> dest` mapping (own line above instead); a trailing inline comment silently corrupts the destination filename, see `../../concepts/tooling.md`. |

**Recorded convention (asked Session 19; re-ask only if policy changes):** park confirmed on-device `code.py` stages as sibling `code_stageN.py`. Replay on the board (optional): comment out the `code.py` manifest line, uncomment `code_stageN.py -> /code.py`, then Copy Files to Board — that overwrites **board** `code.py` only; local `code.py` stays as-is.

## Domain knowledge (central)

- Runtime / allocation → `../../concepts/circuitpython-runtime.md`
- Fonts at small pixel sizes → `../../concepts/fonts.md` (outline fonts fail; 5×5 wants a hand-designed bitmap / MakeCode-style font)
- LED driving (WS2812/NeoPixel output peripheral: RP2→PIO, ESP32→RMT; BananaPi 5×5 index shared, GPIO not) → `../../concepts/led-driving.md`
- Nezha V2 smart motors (I2C `0x10`, 8-byte `FF F9` frame) → `../../concepts/nezha.md` (seeded 2026-09-14; decode evidence-supported, on-device unverified). Unpack: `ai-notes/digests/nezha-v2-motor-protocol.md` (may vanish)

## Resumption point

**2026-09-20 — Session 50: host font-spacing simplified, then Phase 4 cutover.** Unknown → tofu (`_TOFU_INK`); always-between-char spacer; space authored 3 columns. Live `show_string` now `SpacedGlyphColumnFeeder` / `glyph_ink`. `_GlyphColumnFeeder` removed. Phase 5 = on-device re-confirm.

**2026-09-20 — Session 49: familiarization for `text_layout.py` review.** No code edits. Open threads unchanged from Session 48.

**2026-09-20 — Session 48: back after ~1 week.** Exp16 tree clean (`4b61867`, 2026-09-14). No hanging code. Open threads unchanged: (a) font-spacing Phase 4 gated; (b) `code_stage2.py` steps 11/12 not on-device; (c) Stage 3 `planetx` package re-run pending. Next LightTower hardware = analog light on J1/J2, then Nezha V2 motor (decoded, no driver).

**2026-09-14 — Session 47: vendor-vs-Exp16 unpack in `ai-notes/vendor-sources/`.** BananaPi `display.py` is predecessor firmware (index carries; `CharData`/GPIO/`Pixel` do not). PlanetX: only C/D present in `lib/planetx/`; next LightTower = analog light on J1/J2; `nezha.py` in that tree is V1. Home: CONTEXT *Vendor-vs-Exp16 unpack*.

**2026-09-14 — Session 46: Bit-S2 schematic cataloged.** Official electrical PDF is BPI-STEAM `BPI-BIT-Lite-Doc` (`sch/BPI-BIT-Lite-V0.2.pdf`) — vendor alias BIT-Lite/BIT-V2, not the original-bit Hardware repo. Local copy `ai-notes/BPI-Bit-S2_Hardware/` matches GitHub `main` (183103 B). Home: CONTEXT entry *BPI-Bit-S2 schematic*.

**2026-09-14 — Session 45: local vendor snapshots recorded.** `ai-notes/Elecfreaks-Repos/` holds three `source.txt`-pinned trees (two ElecFreaks + BPI-STEAM `MicroPython-Samples`). GitHub is canonical; local is offline alternate; freshness check vs remote HEAD before use — last checked this session, all three match `master`. Home: CONTEXT entry point *Local vendor snapshots*.

**2026-09-14 — Session 44: BPI-STEAM predecessor repos cataloged.** Original bpi:bit (ESP32) + Webduino sources are pointers-only. Durable fact: 5×5 strip **index** matches Bit-S2; **GPIO does not**. Home: `../../concepts/led-driving.md` + `../../crossref/BY_TOPIC.md`.

**2026-09-14 — Session 42: Nezha V2 protocol lifted into persona concepts.** Durable home: `../../concepts/nezha.md` (always-read `_INDEX.md` line). Unpack stays in `ai-notes/digests/nezha-v2-motor-protocol.md`.

**2026-09-14 — Session 41: Nezha2 IIC jacks encoded as shared `planetx.I2C`.** All I2C ports are one bus: P19=SCL, P20=SDA (`board.SCL`/`board.SDA`). Lives in `lib/planetx/ports.py` beside `J1`–`J4`. Font-spacing Phase 4 still gated; Stage 3 `planetx` on-device re-run still pending.

**2026-09-14 — Session 40: Nezha V2 motor protocol digested.** Analysis-only, no driver. Working note: exp16 `ai-notes/digests/nezha-v2-motor-protocol.md`. 8-byte I2C @ `0x10` (`FF F9 …`); MakeCode opcodes preferred over MP where they disagree (absolute-path enum, start 0x5E vs 0x60, speed conversion). LightTower M4 nudges/sweeps map to `0x70`+`0x46`, not combo-drive. Font-spacing Phase 4 still gated; Stage 3 `planetx` on-device re-run still pending.

**2026-09-14 — Session 39: vendor protocol sources recorded.** ElecFreaks MicroPython (`PlanetX_MicroPython`, `EF_Produce_MicroPython`) + MakeCode/PXT (`pxt-PlanetX`, `pxt-nezha2`) linked from exp16 `README.md` § Further reading. Use for pin maps / I2C bytes when adding sensors or motors — not as CircuitPython imports. Font-spacing Phase 4 still gated; Stage 3 on-device re-run of the `planetx` package still pending.

**2026-09-14 — Session 39: Nezha2 J1–J4 live in `planetx.ports`.** `PlanetXButtonSensor(port=J3)` is the Stage 3 constructor. Host pytest **173 passed**. On-device re-run pending. Font-spacing Phase 4 still gated.

**2026-09-13 — Session 33 (this chat, continuing Session 25's `code_stage3.py` draft): naming/design review pass — rename, cleanup, and a registration-pattern refactor all applied, plus an evidence-supported `__slots__`-on-CircuitPython finding.** Alex worked through a batch of questions against the still-undeployed `code_stage3.py`/`lib/buttons.py`. **Applied**: `_make_on_pressed` → `_make_on_pressed_handler` in `code_stage3.py` (documented as a factory function, matching the MakeCode named-handler idiom Alex referenced); removed the unused `self._keys = None` placeholder from `lib/buttons.py.__init__` (grep-confirmed never read anywhere in the repo); replaced the closure-factory + shared `_state` dict + `getattr(buttons, f"on_{letter}_pressed")(...)` loop with a `_PressCounter` callable class (own `letter`/`count` instance attributes) instantiated once per letter and registered via four explicit `buttons.on_*_pressed(press_*)` calls — Alex's chosen combination of two of three proposed alternatives (explicit registration + stateful-object handlers). All changes: `py_compile` clean, pytest 160 passed. **New concept** (`../../concepts/circuitpython-runtime.md`): CircuitPython does not implement `__slots__` at all (confirmed via the CircuitPython repo's own open issue `adafruit/circuitpython#10517`) — declaring it is silently inert on-device even though host CPython pytest would honor it; this is why it wasn't applied to `Buttons` despite being a reasonable-sounding ask. Full detail: `SESSION_LOG.md` Session 33. **Follow-up, same session**: Alex questioned whether `Buttons.run()`'s `await asyncio.sleep(0)` poll was too aggressive — verified against the shipped bundle `asyncio` library's own source (not guessed): `sleep(0)` genuinely busy-spins the CPU whenever it's the sole ready task (skips the scheduler's blocking-poll fallback, since that only triggers when `dt > 0`), not merely "yields once" (a sharper mechanism than the pre-existing yield-once concept). Fixed: `lib/buttons.py` now polls at `_POLL_INTERVAL_S = 0.01` (10ms, half `keypad.Keys`' own 20ms scan interval — confirmed via `docs.circuitpython.org` that the hardware scan is independent of Python's poll rate, so this costs nothing in responsiveness). New concept extension: `../../concepts/circuitpython-runtime.md`. Also simplified `_dispatch`'s double lookup (`_index_to_letter` then `_handlers[letter]`) down to one, after confirming `_index_to_letter` was itself just `_LETTERS` reconstructed as a dict — removed, `_dispatch` now indexes `_LETTERS` directly with an explicit bounds check. Then, on request, stripped "evolution" language (what code *used to be*, e.g. "counterpart to the closure-factory this replaces") from `code_stage3.py`'s `_PressCounter` docstring + registration comment, leaving the file's legitimate K1/K2/Stage-cross-references (current, structural test-scope facts) untouched.

**Milestone, same session: Alex ran Stage 3 on-device and confirmed it — K3 discharged, all 4 original test stages now confirmed on-device.** `lib/buttons.py`'s own `Buttons` class dispatches real events (including onboard A/B, first-ever use on this board) concurrently with a Tier-2 animation, with button-triggered Tier-1 writes correctly cancelling in-flight animations — no lock, per CircuitPython asyncio's cooperative scheduling. Promoted to `../../projects/circuitpython-exp16-planetx/CONCLUSIONS.md` Evidence-Supported. Per Alex's own Session-19 flag, the "ask once all 4 stages are confirmed" trigger fired: whether a unified end-to-end script covering every milestone is worth building. **Answered, same session: no — Alex keeps the per-stage `code_stageN.py` scripts**, switched via `.vscode/cpfiles.txt`'s existing comment/uncomment mechanism (see `SESSION_LOG.md § Open Questions`, resolved). `README.md § Deploy` step 3 gained a one-sentence cross-reference to that switch procedure, which had previously lived only inside `cpfiles.txt`'s own header comment. **Functional state**: this project's declared first-milestone scope (async display + async buttons, PoC-quality with library upgrade path) is now fully discharged end-to-end on real hardware for the first time, with no open action items outstanding.

**2026-09-13 — Session 32 (this chat): font-spacing plan Phases 1-3 executed; Phase 4 gated.** Fresh `git status` at start: `core.py`/`code_stage2.py` clean (committed; `code_stage2.py` already had Session 31's 12-step extension at `63f2081` — drift from the plan's 2026-09-12 "unmodified" snapshot, not a blocker for Phases 1-3). Concurrent dirty files (`cpfiles.txt`, `code.py`, persona memory) left untouched. **Phase 1** (`57ef366`, `77b0594`): host-only `scripts/dal_pendolino3.py` + `verify_dal_font_conversion.py` fetches DAL `pendolino3` at commit `b60953b19634…` (SHA-256 `8f8db78c…`), converts, **475/475 match** against `_COLUMN_MAJOR`; `generate_spaced_font_table.py` writes `font_makecode_5/spaced_glyphs.py` (`[length, ink…]`, 570 B, space length=4). Re-run byte-identical. `glyphs.py` not rewritten. **Phase 2** (`c2fca48`): `glyph_ink` in `font_makecode_5/ink.py`; `tests/test_font_spacing.py`. **Phase 3** (`69d4fd1`): `SpacedGlyphColumnFeeder` in `lib/display/text_layout.py`; `tests/test_text_layout.py`. Host pytest **160 passed**. Exit criteria for Phases 1-3 hold as written; § 3/§ 3b not revised. **Stopped before Phase 4** (`core.py` cutover) per plan § 6 — current call sites: `fit_buf.extend(_glyph_columns(ch))` L959, `feeder = _GlyphColumnFeeder(text)` L995; `show_number` still `await self.show_string(str(n), color, interval_ms, loop)` L1039. Need Alex's explicit go.

**2026-09-13 — Session 30 follow-up (same session record; new chat): both scan items resolved — plan has zero open questions, Phase 1 unblocked pending Alex's go-ahead.** Alex reviewed the loop's two surfaced findings one by one. (1) **§ 3b** (trim algorithm lets 319/9,025 narrow-punctuation two-char strings newly hold instead of scroll) — **accepted as-is, no suppression added.** (2) **§ 3** (build-script data source) — **decided: Position A, refined into a two-script design per Alex's own proposal** (script 1 vendors a static copy of the real DAL `pendolino3` row-byte source, converts it, and verifies/diffs against today's `_COLUMN_MAJOR`, standalone/never-imported; a git-commit checkpoint; then script 2, a copy of script 1, generates the new interleaved `[length, ink...]` format directly from the same vendored source, not from `_COLUMN_MAJOR`). **Empirically verified in-chat before locking in**: fetched the live upstream `MicroBitFont.cpp`, ran the exact row-bytes→column-major conversion `glyphs.py`'s own docstring already documents — **byte-for-byte match, all 475 bytes, zero discrepancy**. **Refined same day, Alex's follow-up call**: rather than vendoring a static byte copy into the repo, script 1 pins an **immutable commit SHA** (`b60953b1963451a6c773c2ed136898b7a2383137`, 2018-09-18 — the last commit to touch this file) and fetches from that commit-specific URL at run time, plus a recorded content-hash check as insurance — deterministic in the sense that matters (a specific commit never changes) without duplicating third-party bytes in-repo; network/GitHub unavailability is an explicitly accepted, deferred risk (precise citation is enough to reconstruct manually later if it ever bites). `plan_v1.0.md` §§ 3/1(T1)/Phase-1/6/7 and `SCAN.md` all updated to reflect both decisions + this refinement; SCAN's Position-A transcription risk downgraded Medium→Low given the empirical result. No further open questions remain in the plan.

**2026-09-13 — Execution authorized, handed off to a fresh chat via `/handoff-exec-prompt`.** Alex asked for the plan-execution handoff prompt in this (planning) chat; per this project's own established convention, execution happens in a separate fresh session, not this one. **Phases 1-3 are the fresh session's autonomous scope** (all new files — build scripts, accessor, feeder — zero touch to the on-device-confirmed `core.py`). **Phase 4 (the `core.py` cutover) remains explicitly gated** per the plan's own § 6 authority map — the fresh session must pause and get a fresh, explicit go from Alex before starting Phase 4, even though overall execution has been authorized; this was not re-litigated or loosened by this handoff, just carried forward faithfully. Phase 5 (on-device) stays entirely Alex's own action. **This resumption pointer is now stale the moment the fresh session makes progress** — that session's own persona-memory updates (SESSION_LOG/CONTEXT) supersede this entry; a future cold read should prefer whichever is more recent.

**2026-09-12 — Session 30 (original entry, kept for the loop-convergence narrative): font-spacing plan-refinement loop ran to completion — converged at iteration 5 of cap 5, promoted to `plan_v1.0.md`, presented to Alex with an open-questions/high-risk scan. No code edits — awaiting Alex's decision on § 3 (build-script data-source: Position A vendor-real-DAL-source vs. Position B defer-to-`_COLUMN_MAJOR`, blocking) and Phase 5 sign-off on § 3b's fit-on-screen behavior-change finding (non-blocking, since resolved above).** Full loop record: `ai-notes/plan/font-spacing-impl/{loop-setup.md, plan_v1.0.md, notes_v0.1.md…notes_v0.5.md, SCAN.md}` (gitignored working folder, not yet cleaned up — pending Alex's read; see `SESSION_LOG.md` Session 30 for the full findings summary).

**2026-09-12 — Session 26 (superseded by Session 30 above — kept for the loop-authorization narrative): font-spacing design (§ 14 of `ai-notes/design/font-inter-glyph-spacing.md`) converged and consolidated (Sessions prior, same day); Alex authorized a plan-refinement loop to turn it into an implementation plan, to run in a separate fresh chat.** Loop authorization locked in exp16 `ai-notes/NOTES.md` § "Plan-refinement loop #2" — carried scope (§ 14 only), cap **5** iterations, reviewer **Grok 4.6 reasoning high** via Cursor Sub-agents (precedent: Session 24 below ran this exact mechanism successfully, same day; Session 30 above records the actual reviewer-model deviation that occurred), explicit standing permission to start without re-asking, and the terminal gate (present matured plan + open-questions/high-risk scan to Alex before any implementation) — **now executed and closed, see Session 30.**

**2026-09-12 — Session 24: display-library documentation cleanup + `led-driving` concept seeded — no code-behavior change, and the pending functional next action is unchanged.** Applied the migrated docstring-quality learnings (four axes: coupling / audience-accessibility / ordering / honesty) to `lib/display/core.py` — 11 doc-only edits via a Grok-4.6 plan-refinement loop (converged iteration 2), `pytest` 149 green. Then a low-risk stale-fact sweep of the whole `lib/display/` package (`README.md` + `core.py`): corrected 8×8/RP2040-heritage leftovers to this board's reality (5×5, BPI-Bit-S2, GPIO18, `WIDTH`-byte, `2*WIDTH`, font node) and **PIO → RMT** (ESP32-S2 drives NeoPixels via the RMT peripheral, not PIO — Alex-confirmed as board owner). `_render_window`'s docstring left **entirely** untouched (Alex-reserved, hand-polished, its stale "16-pixel-wide" example knowingly retained). Logged the whole pass in `Notes/exp14-divergence.md` §3 (doc *restructuring* = back-port to Exp14; the 8×8/RP2040/PIO *fact* corrections = do-NOT-back-port — correct as-is on Exp14's RP2040). Seeded `../../concepts/led-driving.md` (WS2812/NeoPixel output peripheral is MCU-family-specific: RP2 → PIO, ESP32 → RMT; `evidence-supported`). Full detail: `SESSION_LOG.md` Session 24. **Functional state unchanged** — next action is still the font-spacing fix decision / Stage 3, as described in the Session 22 block below.

**2026-09-11 — Session 17: local lib stack re-verified complete; Stage-0 code.py drafted; test plan handed to Alex; no device write yet.**

- Board: **UID `0740D10F1BE9`, CP 10.3.0** (unchanged since Session 14). Still **wiped clean** (only `boot_out.txt`/`settings.toml`/`sd/`, no `lib/`, no `code.py`) as of Session 16 — this session did not touch it (no fresh mount check run; assume unchanged until Alex acts).
- **K2 (PlanetX C/D buttons) is evidence-supported** — do not re-run. **K1 (bundle `asyncio` via this project's own Tier-2 API) is still the load-bearing unknown** — nothing on-device has yet exercised `lib/display/core.py`'s `show_*` methods or `lib/buttons.py`'s `Buttons.run()`; the button-PoC used raw `keypad`+`neopixel` directly, not this project's async layer.
- **Local repo `lib/` — re-verified complete, fresh-eyes, Session 17:** `buttons.py` + `display/` (tracked) + `adafruit_ticks.mpy` + `asyncio/` (untracked, staged via `circup --path`). `circup --path . --board-id bpi_bit_s2 --cpy-version 10.3.0 freeze` shows exactly `adafruit_ticks==1.1.7` + `asyncio==3.1.1`; idempotent re-`install` confirmed no gap. Matches `README.md § Deploy` steps 1-3 exactly. Host pytest re-run fresh: **149 passed** (prior record said 146 — untracked minor drift, not a concern).
- **`code.py.backup` loose end: resolved by disappearance** — no longer exists anywhere in the tree. Separately, the git-tracked `code.py` (button-PoC, commit `b62bc6b`) is deleted from the working tree (`git status`: ` D code.py`) — history + the `ai-notes/` backup snapshot both retain it; not staged/unstaged by the agent, Alex's call if it needs resolving.
- **New Stage-0 `code.py` drafted directly in the local repo this session** (see `SESSION_LOG.md` Session 17 for exact content/rationale) — Tier 1 (sync) only, no `asyncio`, no buttons; isolates the display library's synchronous path before K1 enters. **Proposed, not deployed** — matches `.vscode/cpfiles.txt`'s manifest (`code.py`), so Alex's existing `CP Copy Files to Board` / `sync_files_to_board.py` workflow will pick it up once he reviews and syncs.
- **`.circuitpyignore` does not exist as a real mechanism — corrected, same session.** Verified against the extension's own source + docs (see `../../concepts/tooling.md`); "Copy Libs to Board" copies `lib/` whole-folder, unfiltered, always. Fixed the actual `__pycache__` concern at the source instead: `tests/conftest.py` now sets `sys.dont_write_bytecode = True` (verified: no new `.pyc` writes on a fresh pytest run). Open question for Alex: does he still want a *documentation-only* file (different name) listing dev-only top-level folders, given no tool reads it either way?
- **`readme.txt`** (Alex's empty placeholder) drafted — goal + CP 10.3.0 + bundle libs + public repo URL, small on purpose. Added to `.vscode/cpfiles.txt` so it deploys alongside `code.py`.
- **Standing policy (`../../universal/WORKING_STYLE.md § Domain-Specific`):** local-first, then mirror — no direct-to-board writes by the agent, ever.
- **Next:** Alex reviews the Stage-0 `code.py` + the 4-stage test plan (given in-chat, Session 17), mirrors `lib/` + `code.py` to the board himself, runs Stage 0, reports back what the matrix did — collaborative interpretation, not solo agent analysis. Stages 1-3 (broader Tier 1, then Tier 2 minimal = first real K1 test, then Tier 2 + buttons combined) follow once Stage 0 is confirmed.

**2026-09-11 — Session 17 close-out / phase transition (superseded by Session 19 below — kept for history).** Everything above is now **committed**: `19063d3` "Setup and versioning of 5x5 LED Matrix library on BPI-Bit-S2 (wip)" (Alex's own commit, `alex/display-mvp_5x5`) carries Stage-0 `code.py`, `readme.txt`, `.vscode/cpfiles.txt`, `tests/conftest.py` (`dont_write_bytecode`), and the bundle `.mpy` libs — plus `ad04f03` (requirements.txt + README caveat, prior turn). A useful refinement surfaced in a parallel Session 18 retrospective: `scripts/read_serial_log.py` (autonomous, non-interactive, bounded, never writes) can read a running script's own `print()` output without Alex needing to be present for that specific step — only the **visual** LED-matrix confirmation genuinely needs him physically there in real time. See `../../universal/MONITORING.md` for the exact wording (not yet promoted to a standing directive, still single-incident).

**2026-09-11 — Session 22 (current primary boot reference for the next fresh chat): Stage 2 confirmed on-device; K1 fully discharged; frozen as `code_stage2.py`, not yet committed; a new font-characters/font-spacing investigation flagged, detail pending.** Separate chat from the Session-21-labeled Stage-2-draft session below (note: **two independent concurrent chats both used the label "Session 21"** this same day — one for the Stage-2 draft/refinement work below, another for the icon-storage/`Image.columns` side investigation in `SESSION_LOG.md`; Alex resolved the resulting uncommitted-state overlap himself via a single manual commit, `a5d4e72`, before this session started, so no content was lost — but the number collision itself is a data-quality flag on parallel-chat session numbering, not yet promoted to `MONITORING.md`). This session picked up on a clean working tree (post-`a5d4e72`) and did not re-derive that history from scratch — see `SESSION_LOG.md` Session 22 for the fresh-eyes state check. **Stage 2 ran on the board and Alex confirmed it live**: "the LED Matrix behaves in accordance with what I would expect based on the code" (visual only, no serial-log capture reported this round) — matches the Stage 0/1 confirmation bar. All 10 steps implicitly covered by that confirmation: K1 (asyncio smoke test), the five async `show_*` wrappers, `Image.show_image`/`scroll_image`, and the cancellation-token self-check. Promoted to `CONCLUSIONS.md` Evidence-Supported (K1 now fully discharged, alongside K2). `code_stage2.py` created (byte-identical freeze) and `.vscode/cpfiles.txt` got the matching commented replay line — **not yet committed** (this turn only wrote local files). Alex separately raised a font-spacing investigation (Stage 2 steps 6/7, `"STAGE2"`/`"42"`) and, on request, gave the concrete hypothesis: some characters look 4 columns wide with a gap, others 5 columns wide with none. **Grounded against the actual font source, not taken at face value**: every glyph in `font_makecode_5/glyphs.py` is a fixed 5-column advance; some glyphs (`S`/`A`/`E`/`2`/`K`) have a blank rightmost column baked in as a self-margin, others (`T`/`G`/`4`) fill all 5 columns with none; `core.py`'s `_GlyphColumnFeeder`/scroll path never inserts an explicit spacer column, so all visible spacing is incidental to which glyphs happen to have that blank margin — exactly explaining the `"GE"`/`"42"` collisions Alex flagged. Promoted to `CONCLUSIONS.md` Evidence-Supported. **Later this same chat**, with Alex explicit that a second concurrent chat was now actively editing `lib/display/core.py` ("let's not touch the code here for now"): checked genuine upstream `microbit-dal` source, confirming real hardware *does* reserve an explicit 1-pixel spacer between characters (resolving the "inherited vs. port bug" question above); surveyed the full 95-glyph table (62 margined / 32 full-width / 1 blank); and wrote an analysis-only design proposal scrutinizing Alex's own sketch (ring-resize / boolean-flag / parameter) — `ai-notes/design/font-inter-glyph-spacing.md`, recommending a `_spacer_pending` bool owned entirely by `_GlyphColumnFeeder`, set unconditionally. **Revised 2026-09-12, same chat**: on Alex's follow-up asking whether the spacer could instead be inserted "on demand" (checking whether the just-emitted glyph's own last column was already blank), computed the trailing-blank-column *count* across all 95 glyphs (49 exactly 1, 14 have 2-3, 32 have 0) and used it to show a conditional trigger is strictly cheaper and produces a uniform 1-column gap 85% of the time, at the cost of a scoped departure from DAL's literal bit-for-bit timing. Recommendation flipped to the conditional version (doc § 6); the unconditional one is kept alongside it (§ 6a), not deleted. **Still not implemented** — by the second round, two more concurrent chats (Sessions 23/24) had also touched `core.py`. **Next**: decide with Alex whether the font-spacing fix (§ 6 vs § 6a) or Stage 3 (Tier 2 + buttons combined, the final stage in the original 4-stage breakdown) comes first — not yet sequenced.

**2026-09-11 — Session 21 (superseded by Session 22 above for current state; kept for the Stage-2-draft narrative): Stage 2 (Tier 2 async, display-only) drafted directly into live `code.py`, NOT yet synced to the board.** Fresh-chat handoff (no plan file existed for Stage 2). Fresh-eyes re-verify found zero drift from Session 20's close-out (repo clean, board `code.py` byte-identical to frozen `code_stage1.py`; board `lib/asyncio/`+`adafruit_ticks.mpy` already staged, matching local — K1's dependency precondition already satisfied, no lib-copy step needed before Stage 2). **Buttons-split decision (Alex delegated the call):** Stage 2 stays display-only, no `lib/buttons.py` — combining two still-unverified things (K1's Tier-2 display API + the also-unverified async `Buttons.run()`) would confound failures; buttons wait for a later combined stage where "two concurrent async tasks" becomes the interesting new claim. Matches the original 4-stage breakdown. **Stage 2 script (fresh script per the Stage 0→1 precedent, not appended) — 10 steps:** (1) `asyncio.sleep` K1 smoke test, (2) `show_leds`, (3) `show_icon(Icons.DUCK)`, (4) `show_arrow(Arrows.SOUTH)`, (5) `show_string` short/held, (6) `show_string` long/scrolling, (7) `show_number(42)`, (8) `Image.show_image` two windows of a `create_big_image`, (9) `Image.scroll_image`, (10) cancellation-token self-check (`asyncio.gather` a slow scroll against a sync Tier-1 interrupt, elapsed-time comparison, no visual judgment needed; wrapped in `try/except AttributeError` in case the bundled asyncio lacks `gather`). `Display.forever()` deliberately excluded (redundant with this harness's own custom async setup). Syntax-checked (`py_compile` OK after fixing an accidental line-number-artifact contamination from a bad first draft) + host pytest re-confirmed green (**149 passed**, run from `tests/` — running from the repo root hits a pytest-internal `import pdb`→`import code` collision with the repo's own top-level `code.py` shadowing stdlib `code`; pre-existing environmental quirk, confirmed unrelated to this session's content, not a regression). **Not synced to the board, not committed** (matches the Stage-0/1 precedent: freeze/commit happens only after on-device confirmation).

**Same-session refinements (still 2026-09-11, before any device sync) — `code.py` iterated collaboratively before the first device run:**
- Alex changed live `code.py`'s `set_brightness(0.20)` → `0.10` directly (more comfortable for his eyes) — a local override, not a library-default change (`core.py`'s `BRIGHTNESS` constant is untouched).
- Step 1 (K1 smoke test) now measures actual elapsed time around `await asyncio.sleep(0.5)` via `time.monotonic()` and self-checks against a ±10ms tolerance (serial `[OK]`/`[MISMATCH]`, no visual judgment).
- Step 8 (`Image.show_image`) expanded from 2 offsets to a 5-offset sweep (`0, 3, 5, -2, 8`) covering every case in `_render_window`'s own docstring: aligned-left, aligned-right, in-between/straddling, negative (left overhang), and past-`max_start` (right overhang) — confirmed by reading `_render_window`'s implementation that negative/overhanging offsets are genuinely supported, not just documented.
- Steps 8/9/10 recolored for visual separability (all three render the same shared `_big_image` and were otherwise indistinguishable at a glance): step 8 CYAN → step 9 PURPLE → step 10 WHITE (then cuts to RED mid-scroll on the cancellation check). Reset to CYAN happens at the top of step 8 each cycle via `Image.recolor()`.
- **API rename applied to the library itself** (`lib/display/core.py`, not just the test script): `Image.scroll_image`'s per-frame-step parameter renamed `offset` → `step` — it was reusing `show_image`'s parameter name for a materially different concept (window *position* vs. per-frame *increment*; the scroll always starts at position 0, unlike `show_image` which can start anywhere including negative/overhanging). Prior art discovered in the process: Exp14's own `working-docs/refactor-round-todos.md` #12(a) had already flagged this exact ambiguity (2026-06-14, unapplied) — cross-project promotion to `CODING_PRINCIPLES.md` (`[user]` scope, no sign-off needed per M3's 2-occurrence rule). See `universal/CODING_PRINCIPLES.md § Core Principles` + `crossref/BY_PATTERN.md`.
- **New artifact**: `Notes/exp14-divergence.md` — a living, current-state-only record of every design/API divergence between exp16's `lib/display/` and exp14's origin copy (wiring LUT, font backend, `render_pattern` hot-path fusion, icon corrections, the `offset`→`step` rename), explicitly written to inform an eventual return to 8×8 on an RP-class board. Indexed at `crossref/BY_TOPIC.md`. Distinct from `Notes/student-api-portability.md` (that one is student-sketch-facing; this one is maintainer/internals-facing).
- Host pytest re-confirmed green after every change (149 passed, run from `tests/`), `py_compile` clean throughout.

- **`lib/display/core.py` docstring + validation pass on `show_string` and its Tier-2 siblings** (before any device sync, still same day): `show_string`'s docstring reordered to explain the typical (scrolling, >1-char) case first, then the fit-on-screen hold as "five step-durations" of that same unit — reads naturally instead of the `interval_ms * 5` multiplier appearing out of nowhere. Traced that multiplier's origin: present unchanged since the very first AI draft of this file (`9ef8ac6`, 2026-04-16, Exp14), no documented derivation ever, identical in both libraries — disclosed as an arbitrary "long enough to read" constant rather than implying a rationale that doesn't exist. **Negative `interval_ms`/`ms` now raises `ValueError`** on `show_string`, `show_leds`, `show_icon`, `show_arrow`, and `pause` (checked before any side effect, i.e. before `_acquire()`/render) — closes a silent-coercion gap where negative values previously fell through to "no hold" unnoticed; `show_number` inherits the fix via delegation to `show_string`. New `[user]`-scope directive recorded: *avoid frequent `--` in human-facing docs, acceptable in AI-facing ones* (raised mid-review of the `show_string` rewrite, which had been `--`-dense) — applied to everything touched this turn; **not yet** swept across the rest of the file at that point (pre-existing `--` count: ~54 `core.py`, ~34 its `README.md`, ~44 exp16 `code.py`). Host pytest re-confirmed green after each edit (149 passed).

- **Two further docstring triage passes on `show_string`** (same day, immediately after the above), applying/extending *Public object descriptions should describe public contract, not private coupling* (`CODING_PRINCIPLES.md`): (a) the "Ring sizing" paragraph — mostly ring-buffer/scroll-in/scroll-out mechanism, moved to a comment near `ring = bytearray(WIDTH)` / the `trailing_blanks > WIDTH` check; kept one caller-relevant sentence ("a non-looping scroll ends exactly when the last meaningful column has left the screen") standalone in the docstring. (b) the "typical case" paragraph — Alex's framing this time was *accessibility* ("needs to be accessible to a coding student"), not coupling: the `_GlyphColumnFeeder`/ring-buffer mention and an O(WIDTH)-memory Big-O clause both moved to the same internal comment, leaving the docstring as just "scrolls one column per `interval_ms` step." Surfaced a second calibration axis on the coupling principle (audience-appropriate language, orthogonal to public/private coupling) — logged as a 1-instance addendum, not yet promoted into Evaluate/Act.
- **Retroactive `--` sweep, on request** ("Now, let's clean up the frequent usage of `--`"): scoped by Alex to the 3 originally-flagged files only (`lib/display/core.py`, `lib/display/README.md`, exp16 `code.py`; declined the broader `lib/display/*.py`), excluding `Notes/Button_chat.md` (a pasted ChatGPT transcript) when offered as an option. Categorized all raw `--` hits first: real prose em-dash substitutes (`core.py` 33, `README.md` 21, `code.py` 44 — bulk `" -- "` → `" — "` replace plus a few manual line-wrapped cases) vs. incidental matches left untouched (`# ---...---` section dividers, banner-style docstring headers, Markdown table separator rows, Mermaid `-->` arrows, one Markdown anchor link). Frozen `code_stage0.py`/`code_stage1.py` excluded per the standing no-modify constraint, not per this directive. `py_compile` clean, pytest 149 passed after.

- **f-strings vs `.format()`** (same day): Alex flagged `create_image`'s `.format()`-built `ValueError` message as less pythonic, asked whether there's a notable performance difference before deciding. Investigated rather than assumed: on CircuitPython/MicroPython, f-strings are a **lexer-level syntax rewrite to `.format()`** (`f"{a}"` → `"{}".format(a)`, not dedicated bytecode like on CPython) — so the two are bytecode-identical on-device, zero perf difference, pure readability/idiom choice. New `[user]`-scope directive recorded in `CODING_PRINCIPLES.md` with the sourced grounding note. Converted both `.format()` sites in `lib/display/core.py` (`create_image`, `create_big_image`) to f-strings; left two structurally-similar `.format()` calls in `tests/test_pattern_codec.py` untouched (test helper code, out of the flagged scope). `py_compile` clean, pytest 149 passed.

**Next:** Alex runs `CP Copy Files to Board` himself, then collaborative visual + autonomous-serial confirmation of all 10 steps together.

**2026-09-11 — Session 20 close-out (superseded by Session 21 above for current state; kept for the Stage-1 confirmation narrative): Stage 1 (broader Tier 1) fully confirmed on-device — serial + visual — matching Stage 0's confirmation bar; frozen + committed. Next = design Stage 2.** Fresh-chat boundary re-verify found no drift from Session 19's handoff; `code.py`'s inline `_DIAMOND`/`_RING_PATTERN` pattern strings reformatted to multiline triple-quoted grids (human-readability only, zero behavior change). Alex then ran `CP Copy Files to Board` himself and watched Stage 1 live — matches expectations, aside from a color-tuning caveat (`display.YELLOW` has an orange tinge, `display.ORANGE` is visually indistinguishable from `display.colorwheel(0)`/true red — tracked as a deferred TODO in `CONCLUSIONS.md` + `SESSION_LOG.md § Open Questions`, not blocking, revisit only after all 4 stages progress). Autonomous read-only serial capture (`scripts/read_serial_log.py`) independently confirmed all 6 steps print cleanly each cycle including the step-3 `get_pixel` self-check (`[OK]`) — that run needed `required_permissions: ["all"]` since the default sandbox blocked the USB serial device open; expect the same next time. **Stage 1 frozen as `code_stage1.py`** (byte-identical to the confirmed board copy) and **committed**: `94e54ca` (code: freeze + `.vscode/cpfiles.txt` replay line) + `83e51f1` (persona memory). Working tree fully clean after both commits — no open uncommitted-memory question remains. **Next: design Stage 2** (Tier 2 minimal — the first real test of K1, bundle `asyncio` via this project's own Tier-2 async API: `lib/display/core.py`'s `show_leds`/`show_icon`/`show_arrow`/`show_string`/`show_number`/`show_image`, `829-960`ish; possibly `lib/buttons.py`'s `Buttons.run()` if Stage 2 also wants a minimal buttons touch, though the original 4-stage breakdown put buttons+Tier2 combined at Stage 3, not Stage 2 — confirm the split with Alex before assuming). No Stage 2 draft exists yet anywhere (live `code.py` still holds only the confirmed, frozen Stage 1 content) — this is a from-scratch design task, not a continuation of an existing draft.

**2026-09-11 — Session 19 close-out (superseded by Session 20 above for current state; kept for the full Stage-0/Stage-1 narrative).** Alex ran the Session-17 mirror steps (`CP Copy Libs to Board` + `CP Copy Files to Board`); board confirmed byte-identical to the local repo. An autonomous-serial-capture timing race (one-shot `code.py` had already fallen back to the REPL before capture started) was found and fixed by construction — `code.py` now loops (`while True`) instead of running once; validated clean on retry (two full cycles, zero drops). **Stage 0 is now fully confirmed on-device** (serial + Alex's live visual pass) and **committed** (`b99152b`): LUT origin top-left, Tier-1 sync rendering path, and a new brightness-floor edge case (0.01-and-below visually off, 0.02 lowest still lit) — all promoted to `CONCLUSIONS.md`. A same-turn regression (per-cycle state reset had accidentally zeroed out step 4's brightness contrast) was caught by Alex reading the code and fixed before commit. **Stage 1 (broader Tier 1) is drafted directly into live `code.py`, additive-only** (`render_pattern`, `render_arrow`, `get_pixel` self-check, second icon swept through all 4 rotations, `create_image`, `colorwheel`) — **not yet synced to the board**. Alex asked to trim the script down to just these 6 (dropped the verbatim-repeated Stage 0 steps that had been renumbered 7-12 in the same loop) so a human re-running it doesn't have to re-watch already-confirmed behavior; `code.py` shrank 174→121 lines, its own steps renumbered `1/6`..`6/6`. Recorded as a general `[user]`-scope test-design preference in `../../universal/CODING_PRINCIPLES.md` (*milestone/stage tests are additive, not repetitive*) — that entry also carries Alex's forward-looking, not-yet-decided note: once all 4 stages are confirmed, ask him whether a single unified end-to-end script (covering every milestone in one run) is worth building for a future maintainer. Stage-0's confirmed script stays frozen as a sibling `code_stage0.py` (see § Recorded convention above); `.vscode/cpfiles.txt` carries a commented-out `code_stage0.py -> /code.py` replay line if ever needed. **Board state:** last independently re-verified in Session 19 (byte-identical mirror confirmed); not re-checked since the Stage-1 draft (device not yet touched for it). **Next:** Alex runs `CP Copy Files to Board` (libs unchanged, one less reset than a full mirror), reports back; capture serial autonomously + confirm the 6 steps visually together; on success, commit Stage 1. One item still open: whether/when to commit the accumulated persona-memory files (several sessions' worth now uncommitted). The `BRIGHTNESS` history discrepancy (previously open) is **resolved** — see header above.

**2026-09-12 — Session 25 (concurrent chat, additive only): `code_stage3.py` (Stage 3, Tier 2 + buttons combined) drafted — a new file, no existing file touched.** Ran alongside another chat actively iterating on `code.py`'s Stage 2 milestone (Alex's explicit instruction: don't touch `code.py`); sidestepped the shared-file collision this project's concurrent-chat pattern keeps hitting by only adding a file that didn't exist before. Full design rationale (new unknown **K3**: does `lib/buttons.py`'s own `Buttons.run()` — not K2's bypassing raw-`keypad` PoC — dispatch real events concurrently with an in-flight Tier-2 animation, and does a button-triggered Tier-1 write correctly cancel it) in `SESSION_LOG.md` Session 25. `py_compile` + host `pytest` (149 passed) both clean; not synced to the board, not committed, not yet even attempted on-device — this is a fresh draft, review-then-run is the next step whenever Alex is done with the parallel Stage-2 work.

**Session 11/15 coverage-discussion reconciliation (older, now moot):** resolved in Session 16 — `0740D10F1BE9` confirmed as the round-1 target.
