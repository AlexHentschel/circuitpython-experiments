# Conclusions — circuitpython-exp16-planetx

Status tiers: `unverified` · `evidence-supported` · `disputed` · `invalidated`.

## Evidence-Supported

| Finding | Scope | Evidence | Date |
|---------|-------|----------|------|
| BPI-Bit-S2 5×5 WS2812 logical (col, row) with origin top-left maps to strip index `row + 20 - column * 5` (column-major, right-to-left physically). **On-device origin now confirmed** (Session 19): `set_pixel(0, 0, WHITE)` visually lit exactly the top-left LED, matching the documented LUT. | `[exp16]` `[cross-experiment]` (from exp09) | Formula + ASCII map in `2026-02_Exp09_…/lib/display_v0.py` and `lib/microbit.py`. On-device confirmation: Alex's live visual observation of Stage-0 `code.py` step 2/5 on UID `0740D10F1BE9`, 2026-09-11. | 2026-09-03; confirmed 2026-09-11 |
| **Stage 0 on-device Tier-1 (sync) rendering path confirmed end-to-end** on UID `0740D10F1BE9`: `import display` succeeds (hardware-import branch, LUT build, NeoPixel buffer alloc); `fill`/`clear_screen`/`set_pixel`/`render_icon` all write real, correctly-shaped pixels; `set_rotation(90)` visibly turns a rendered icon. `set_brightness`'s visual effect was *not* cleanly demonstrated this round (a same-session test-harness bug made it a no-op — see Session 19 log); the underlying setter call itself did not error. | `[exp16]` | Serial capture (`scripts/read_serial_log.py`, all 5 step markers per cycle, zero errors across 2+ cycles) + Alex's live visual confirmation ("consistent with my expectation of the code"), 2026-09-11. | 2026-09-11 |
| **`set_brightness()` practical floor on the onboard NeoPixel (WS2812), UID `0740D10F1BE9`, is authoritative per Alex:** brightness **0.01 and below is visually indistinguishable from off**; **0.02 is the lowest level still visibly lit**. Tested with a full-matrix solid-color fill (not a single pixel), sweeping `0.05 → 0.04 → 0.03 → 0.02 → 0.01 → 0.005 → 0.0` on the same buffer. Likely mechanism (not independently verified): WS2812 PWM duty-cycle/rounding at very low brightness×color-component products. `0.05` is **not** the library's default (that's `lib/display/core.py`'s `BRIGHTNESS = 0.20`, see the Invalidated row below) — it's only ever an ad-hoc dimming-demo value in the test script's step 4. | `[exp16]` | Alex's live visual observation of Stage-0 `code.py` step 6/6, 2026-09-11. | 2026-09-11 |
| Stock CircuitPython **10.3.0** `bpi_bit_s2` firmware includes `_asyncio`, `keypad` / `keypad.Keys` / `keypad.EventQueue`, `rainbowio`, and frozen `neopixel`. User-facing `asyncio` is **not** in the firmware (see `concepts/circuitpython-runtime.md`). | `[exp16]` | 10.3.0 support matrix board cell `docs.circuitpython.org/en/10.3.0/shared-bindings/support_matrix.html` (BPI-Bit-S2); `ports/espressif/mpconfigport.mk` `CIRCUITPY_FULL_BUILD ?= 1`; `py/circuitpy_mpconfig.mk` `CIRCUITPY_KEYPAD ?= $(CIRCUITPY_FULL_BUILD)`; board `mpconfigboard.mk` `FROZEN_MPY_DIRS += …/Adafruit_CircuitPython_NeoPixel`. **On-device `help("modules")` still P8.** | 2026-09-04 |
| CircuitPython `board.IO13` / `IO14` on this firmware **are** goldfinger P13/P14 (GPIO36/GPIO37). `board.NEOPIXEL` = GPIO18. Photoresistors are `board.LUM1`/`LUM2` (GPIO12/13), not `IO13`/`IO14`. | `[exp16]` | `ports/espressif/boards/bpi_bit_s2/pins.c` tag `10.3.0`: `IO13`→`pin_GPIO36` (also `SCK`/`D13`); `IO14`→`pin_GPIO37` (also `MISO`/`D14`); `NEOPIXEL`→`GPIO18`; `BUTTON_A`/`BUTTON_B`→GPIO38/33; `LUM1`/`LUM2`→GPIO12/13. Unifies Exp09 names with BananaPi GPIO numbers (different namespaces). | 2026-09-04 |
| Exp09 5×5 `_FONT` is derived from pitchfork-5x5 (**GPLv3**). | `[exp16]` | Source comment in `2026-02_Exp09_…/lib/microbit.py` (~line 310) + `generate_font.py`. | 2026-09-04 |
| Lancaster DAL `MicroBitFont.cpp` / `pendolino3` is **MIT** (Copyright 2016 BBC; Lancaster by arrangement with the BBC). | `[exp16]` `[domain:fonts]` | File header + https://github.com/lancaster-university/microbit-dal/blob/master/LICENSE (fetched 2026-09-04). Apache-2.0 hypothesis **invalidated**. | 2026-09-04 |
| BananaPi Docs site-wide footer licenses page content (including `bpi_bit_v2_goldfinger.jpg` and `bpi_bit_v2_interface_en.jpg`) as CC BY-SA by BananaPi. Unmodified copies may sit in a public experiment as collection items; ShareAlike does not infect the rest of the tree. Site does not print a CC version. | `[exp16]` | Footer text on https://docs.banana-pi.org/en/BPI-Bit-S2/BananaPi_BPI-Bit-S2. Sidecars: exp16 `Notes/*.jpg.license`. README caption attributes the interface figure. | 2026-09-04 |
| Official 5×5 WS2812 sequential list matches Exp09 **and** the original ESP32 bpi:bit; Bit-S2 data pin GPIO18 vs original GPIO4 — **index shared, pin not** (durable form + `index = row + 20 - column * 5` formula: `../../concepts/led-driving.md`). | `[exp16]` `[cross-experiment]` | BananaPi Bit-S2 page “5*5 LED Sequential List” (2026-09-04) + [BPI-BIT-Hardware `readme_en.md`](https://github.com/BPI-STEAM/BPI-BIT-Hardware/blob/master/readme_en.md) sequential table (2026-09-14). | 2026-09-04; predecessor corroboration 2026-09-14 |
| Original **bpi:bit** (ESP32) GPIO / I2C maps **do not** transfer to BPI-Bit-S2; the matrix strip **index** does (durable form + the `heartbeat.py`-GPIO18-is-a-status-LED-not-the-matrix trap: `../../concepts/led-driving.md`). | `[exp16]` `[cross-experiment]` | Hardware repo occupancy table (matrix GPIO 4, buttons 35/27, MPU SCL/SDA GPIO 22/21, lights 36/39) vs CP 10.3.0 `bpi_bit_s2` `pins.c` (NEOPIXEL GPIO18, BUTTON_A/B GPIO38/33, LUM1/LUM2 GPIO12/13, SCL/SDA = goldfinger P19/P20). `microbit/__init__.py` hard-codes the original map. | 2026-09-14 |
| Official **BPI-Bit-S2 schematic** is BPI-STEAM [`BPI-BIT-Lite-Doc`](https://github.com/BPI-STEAM/BPI-BIT-Lite-Doc) `sch/BPI-BIT-Lite-V0.2.pdf` (MIT). Same board as CircuitPython `bpi_bit_s2`; vendor also calls it BIT-Lite / BIT-V2 / wiki Bit_Lite. **Not** the original-bit `BPI-BIT-Hardware` PDFs. | `[exp16]` | BananaPi Bit-S2 docs “BPI-Bit-S2 schematic” → that blob; title block `ESP32-S2-BIT Lite` / MCU `ESP32-S2FN4R2`; prior filename `BPI-BIT-V2-V0.2.pdf` (commit `775cf99`). Local copy: exp16 `ai-notes/BPI-Bit-S2_Hardware/` (size 183103 = GitHub, 2026-09-14). Pin decode not claimed. | 2026-09-14 |
| BananaPi original-bit `microbit/display.py` **`CharData` is not** Lancaster DAL `pendolino3`. Sequential 25-int 0/1 glyphs vs five row-bytes. Exp16 font stays DAL. | `[exp16]` `[domain:fonts]` | Local `MicroPython-Samples/microbit/display.py` `CharData` (`c03ed50`) vs Exp16 `font_makecode_5/` + `concepts/fonts.md`. Unpack: `ai-notes/vendor-sources/display-vs-exp16.md`. | 2026-09-14 |
| PlanetX **light** sensor driver accepts analog **J1/J2 only** (micro:bit P1/P2). Same jack rule as other analog PlanetX modules (trimpot, soil, …). Digital C/D buttons remain J1–J4. | `[exp16]` | `PlanetX_MicroPython/light.py` (`268740c`): `RJ_pin == J1` → `pin1`, `J2` → `pin2`; no J3/J4 branch. Matches Exp16 `Port.analog_silk` on J1/J2. On-device lux curve unverified. Unpack: `ai-notes/vendor-sources/planetx-vs-lib.md`. | 2026-09-14 |
| Host visual-fixture tests pin the 5×5 LUT (corners, interior, rotations) to that sequential grid. | `[exp16]` | `tests/test_geometry.py` `_VISUAL_ROT0` (not the wiring formula). On-device LUT still P8. | 2026-09-04 |
| DAL `pendolino3` row-bytes convert to Exp14 column-major via `(row_byte >> (4-c)) & 1`. MIT notice vendored beside the table. | `[exp16]` `[domain:fonts]` | DAL header `!` example + Exp16 `lib/display/font_makecode_5/` + `tests/test_font.py`. | 2026-09-04 |
| Exp09 `Image.GHOST` / `Image.TRIANGLE_LEFT` are **not** the MakeCode grids. Exp16 `GHOST` and `LEFT_TRIANGLE` now follow MakeCode (rounded head; right-triangle with full base). Other icons remain Exp09 ports. | `[exp16]` | Exp09 `lib/microbit.py` vs MakeCode screenshots 2026-09-04. Fixtures: `tests/test_icons_data.py` `MAKECODE_ICON_PATTERNS`. | 2026-09-04 |
| Exp16 `lib/` and `tests/*.py` contain **no** absolute host filesystem paths. Host pytest locates `lib/` via `Path(__file__)` in `tests/conftest.py`. There are no experiment shell scripts. A different checkout’s venv needs **no code edits** — only a local interpreter with pytest. Absolute `/Users/alex/…` paths are prose (example in `tests/README.md`, locks in `ai-notes/NOTES.md`). Shared workspace CircuitPythonSync is **outside** this experiment. | `[exp16]` | Grep 2026-09-04: no `/Users/`, `/Volumes/`, `PythonVEs` under `lib/` or `tests/*.py`. | 2026-09-04 |
| **This plugged-in unit** (UID `0740D10F1BE9`) ran CircuitPython **10.3.0** `bpi_bit_s2` as of 2026-09-11 Path A UF2 copy (was 10.0.3). Other BPI-Bit-S2s in the fleet are **not** assumed to match; re-read `boot_out.txt` per board. | `[exp16]` | `/Volumes/CIRCUITPY/boot_out.txt` after Path A: `Adafruit CircuitPython 10.3.0 on 2026-08-31; BPI-Bit-S2 with ESP32S2`; USB `303A:80E6`. Prior 10.0.3 on same UID. Alex: multiple boards, do not share version. | 2026-09-11 |
| Path A (CP `.uf2` onto `BITS2BOOT` with TinyUF2 already **0.35.0**) upgraded this unit 10.0.3 → 10.3.0 and **kept** CIRCUITPY (`code.py`, `lib/`, `sd/`, `settings.toml`); prior LED-icon sketch still ran. | `[exp16]` `[domain:tooling]` | Alex report + `boot_out.txt` 10.3.0 same UID; listing still had those files. TinyUF2 not rewritten. | 2026-09-11 |
| UID `0740D10F1BE9` CIRCUITPY FAT volume is **983040 bytes** (~941 KiB; ~745 KiB free) = TinyUF2 `ffat` 960K (same on dual-OTA and no-OTA 4MB tables); `Flash Size: 0x002C0000` is `ota_0`, **not** CIRCUITPY. Prior “pre-0.33 CIRCUITPY would be larger” claim **invalidated**. Tables: `../../concepts/tooling-4mb-partitions.md`. | `[exp16]` `[domain:tooling]` | TinyUF2 0.32/0.35 CSVs (`ffat` 960K both); `diskutil` 2026-09-11; Adafruit Learn CP10 4MB page. | 2026-09-11 |
| BPI-Bit-S2 TinyUF2 mass-storage volume is **`BITS2BOOT`**; enter via tap **RESET** then **BOOT** (~1 s, matrix purple), ROM mode = hold **BOOT**/tap **RESET**; this board is **`esp32s2`** (BananaPi pages saying `esp32s3` are wrong-chip). Full recipe: `../../concepts/tooling.md`. | `[exp16]` `[domain:tooling]` | Board page + BPI-Steam `update_circuitpython.html` (BITS2BOOT / RST then BOOT); circuitpython.org ROM-mode recipe; TinyUF2 0.35.0 + CP 10.3.0 URLs HTTP 200 on 2026-09-11. | 2026-09-11 |
| Pre-Phase-4 scrolled/held text inserted **no** inter-character gap, so full-width DAL glyphs merged (`G`/`E`, `4`/`2`). **Invalidated as a description of the live path 2026-09-20** — see the next row. The §6–§14 design rounds (conditional spacer, `needs_spacer`, space width 4) did not ship; full prose is git `4a337f4` this file. Historical spec: exp16 `ai-notes/design/font-inter-glyph-spacing.md` §14. | `[exp16]` `[domain:fonts]` | Stage 2 visual 2026-09-11; DAL `updateScrollText` advances by `width + 1`. | 2026-09-11; invalidated 2026-09-20 |
| **Live `show_string` always inserts one spacer between characters; unknown glyphs draw tofu (`ink._TOFU_INK`); space is 3 blank columns.** `glyph_ink` returns `bytes` only — `needs_spacer` dropped because a blank glyph no longer has to suppress the following spacer. Interior `"A B"` stays 5 gap columns (same as the former 4-column space with a spacer only after ink). Trailing and leading space shrink by 1. `☐` (U+25A1) is not in ASCII 32–126; tofu is stripped `SMALL_SQUARE` ink (`0e 0a 0e`), stored directly, pinned against `icons.py` `# 38`. Fit probe and scroll share `SpacedGlyphColumnFeeder`. `_GlyphColumnFeeder` removed. Phase 5 = on-device re-confirm. | `[exp16]` `[domain:fonts]` | Host pytest 2026-09-20: `tests/test_font_spacing.py`, `tests/test_text_layout.py`. Cutover: `core.py` `show_string`. Generator `SPACE_AUTHORED_WIDTH = 3` from pinned DAL. | 2026-09-20 |
| **`show_number(n: int, ...)`'s type hint overstates its own contract — implementation already accepts any `str()`-able value without crashing, but its output quality for float/bool/nan is undocumented and unconsidered (not yet fixed).** Surfaced 2026-09-12 while analyzing the font-spacing doc's `"27.3"` example (a decimal sensor readout). `show_string` already defensively `str()`-coerces its own input, so `show_number` cannot crash on a float, `bool` (an `int` subclass — `show_number(True)` already type-checks *today*, producing `"True"` not `"1"`), big int, or `nan`/`inf` — the real gap is display quality (host-CPython `str(1/3)` → 18-char `"0.3333333333333333"`; scientific notation for very large/small floats) and doc-truth, not correctness. Grounded CircuitPython's float precision against 3 independent GitHub issues (not asserted from memory): default 30-bit float (`MICROPY_OBJ_REPR_C`, narrower than IEEE754 single precision) — confirmed for RP2040/SAMD21-family ports, **not directly confirmed for this project's ESP32-S2/RP2350 targets** (flagged unverified, needs on-device check); host-CPython `str(float)` testing does not reliably predict on-device output. Design doc with routes A-D (widen type hint/doc truth; bounded `decimals` param; guard nan/inf/bool; no-op) — exp16 `ai-notes/design/show-number-numeric-types.md`. **Decided 2026-09-12 (same day, round 2):** Alex approved A+B; **explicitly rejected C** — nan/inf may be legitimate output-domain values and are valuable for debugging even as bugs; bool-as-text is over-permissive but not unsafe — no guards added anywhere, existing fully-permissive `str()`-based acceptance preserved. Alex also requested a `scientific: bool | None = None` companion to `decimals`; approved after testing surfaced two real, now-documented interaction gotchas: (1) supplying either `decimals` or `scientific` formats a `bool` as its numeric value (`"1.00"`), not `"True"`/`"False"`; (2) both convert their operand through `float` first, so very large `int`s (beyond ~2^53) can silently lose exact-digit precision versus plain `str(n)`. **Revised same day (round 3):** Alex reconsidered the bool position specifically (nan/inf reasoning untouched) — `decimals`/`scientific` should be ignored entirely for `int` inputs (structurally removes gotcha 2, not just documents it: `int` never reaches the float-converting format spec), and `bool` should be explicitly rejected (`TypeError`) rather than tolerated, since a type check is needed anyway to route int-vs-float — `str` is excluded for free by the same check. +2 lines, O(1)/nanosecond added cost, net correctness improvement (removes gotcha 2 outright; replaces gotcha 1's silent surprise with an explicit error). Not implemented; analysis-only. | `[exp16]` `[domain:circuitpython-runtime]` | Code read (`core.py` `show_number` L1007-1021, `show_string` L930-934 confirming the defensive `str()` coercion); `adafruit/circuitpython` GitHub issues #9237, #5802, #583 + `micropython/micropython` #15236 (fetched 2026-09-12). | 2026-09-12 |
| **Color constants in `lib/display/_constants.py` are perceptually off and need tuning (not yet done).** `YELLOW = (255, 150, 0)` renders on-device as a warm yellow with a visible orange tinge (Alex: "the warmth is fine, but the tinge isn't"); `ORANGE = (255, 40, 0)` renders as **plain red**, indistinguishable from `display.colorwheel(0)` (an independent, library-external "true red" baseline: `0xFF0000`) — its green channel (40) is too low to read as orange against the WS2812's actual emission. Both observations are consistent with the raw RGB values themselves (orange's green channel is only 16% of max). **Not fixed this session — tracked as a TODO for a later color-tuning pass** (iterate the RGB constants against how they actually look on this specific WS2812/diffuser, not just the numeric values), see `SESSION_LOG.md § Open Questions`. | `[exp16]` | Alex's live visual observation of Stage-1 `code.py` steps 4/6 (`Icons.HAPPY` in `YELLOW`), 5/6 (ring pattern in `ORANGE`), and 6/6 (`colorwheel(0)` sweep, same cycle) confirming ORANGE vs. colorwheel-red is a no-visible-difference comparison, 2026-09-11; cross-checked against `lib/display/_constants.py` lines 34-35. | 2026-09-11 |
| **Stage 1 (broader Tier 1) fully confirmed on-device**, UID `0740D10F1BE9`: all 6 steps (`render_pattern`, `render_arrow`, `get_pixel` self-check, `HAPPY` icon at 4 rotations, `create_image`+`render_icon`, `colorwheel` sweep) match expectations per Alex's live visual pass ("what I would expect based on the code," aside from the separately-tracked color-tuning TODO above). Serial self-check for step 3 independently confirms the read-back path: `set_pixel(2,2,MAGENTA) -> get_pixel(2,2) = (255, 0, 20) [OK]`. | `[exp16]` | Alex's live visual confirmation, 2026-09-11 + autonomous serial capture (`scripts/read_serial_log.py`, read-only, 28s window) same session: cycle 87 complete with all 6 step markers, zero drops/errors, `get_pixel` self-check printed `[OK]`. | 2026-09-11 |
| **Stage 2 (Tier 2 async, display-only) fully confirmed on-device — K1 discharged.** All 10 steps of `code.py` ran on UID `0740D10F1BE9`: bundled `asyncio` runs at all (step 1, the actual first exercise of K1); the async `show_leds`/`show_icon`/`show_arrow`/`show_string`/`show_number` wrappers render correctly (steps 2-7); `Image.show_image`'s 5-offset window sweep (aligned/straddling/negative/overhang) and `Image.scroll_image`'s full-width animation both work (steps 8-9); the cancellation-token contract (`Display._acquire`/`_is_cancelled`) correctly cuts a slow scroll short when an ordinary Tier-1 write fires concurrently (step 10). Alex: "the LED Matrix behaves in accordance with what I would expect based on the code" — matches the Stage 0/1 confirmation bar (no serial-log capture reported this round, visual confirmation only). Frozen as sibling `code_stage2.py`. | `[exp16]` | Alex's live visual confirmation, 2026-09-11. | 2026-09-11 |
| Nezha2 jacks J1–J4 map to goldfinger P-numbers then `board.IOn` (not ESP32 GPIO n). Encoded in `lib/planetx/ports.py`. J1=(P1,P8) analog P1; J2=(P2,P12) analog P2; J3=(P13,P14); J4=(P15,P16). Driver order, not the jack-drawing stack (J3 draws P14 above P13). | `[exp16]` | Nezha 202 jack figure (J1–J4 → P-numbers) + `Notes/bpi_bit_v2_goldfinger.jpg` (Pn → GPIOx) + existing `pins.c` row (`IOn` = goldfinger Pn). J3 matches K2 (`IO13`/`IO14` = C/D). | 2026-09-14 |
| **`Pn` is the BBC micro:bit edge-connector pin-naming standard, not an ElecFreaks- or BPI-Bit-S2-local label — "goldfinger P13" and ElecFreaks' Nezha2 jack-figure "P13" name the same standardized contact, not two labels that coincidentally match.** Confirmed against the micro:bit hardware team's own edge-connector docs (`tech.microbit.org/hardware/edgeconnector`, `tech.microbit.org/hardware/schematic`): on a real micro:bit V2, `P13`=nRF52833 `P0.17`, `P14`=`P0.01` — fixed contact positions on the standardized 25-pin edge connector, independent of any one vendor. BPI-Bit-S2's "goldfinger" is its own implementation of that same connector standard (ESP32-S2 behind it, so `P13`→`GPIO36`, not `P0.17` — the real, still-live caveat is `Pn` vs `GPIOn`, not `Pn`-vendor-A vs `Pn`-vendor-B). ElecFreaks' Nezha2 documents its own RJ11-jack wiring using that same shared `Pn` namespace — it isn't inventing or coincidentally reusing the numbers. **Generalizes beyond this project**: any micro:bit-form-factor board (this project's own predecessor Exp09, and Exp15's RP2350-Bit-Board) that claims edge-connector compatibility is expected to expose the same `Pn` at the same physical position, whatever its own MCU/GPIO numbering underneath — candidate for a `[cross-experiment]` concept once a second project's own edge-connector work touches it (see `concepts/_INDEX.md § Candidate domains`). | `[exp16]` `[cross-experiment candidate]` | `tech.microbit.org/hardware/edgeconnector` + `/hardware/schematic` (BBC micro:bit foundation's own pin tables), cross-checked against this project's existing `pins.c`-sourced goldfinger row above. | 2026-09-20 |
| All Nezha2 IIC jacks are **one** I2C bus (not four independent buses): silk P19 = SCL, P20 = SDA. Encoded as `planetx.I2C` (`I2CBus`). Pin bind is the same silk → `board.IOn` map as J1–J4 (`IO19`/`IO20` = `board.SCL`/`board.SDA` on this firmware). | `[exp16]` | ElecFreaks Nezha 202 IIC figure (P20→SDA, P19→SCL, VCC, GND on every IIC jack) + goldfinger P19=GPIO16 / P20=GPIO15 + existing `pins.c` SCL/SDA aliases. | 2026-09-14 |
| **K3 fully discharged — Stage 3 (Tier 2 async display + buttons combined) confirmed on-device; all four original test stages now complete.** `lib/buttons.py`'s own `Buttons` class — not K2's bypassing raw-`keypad`+`neopixel` PoC — dispatches real hardware events via its own `run()` pump for all four buttons, including the onboard A/B pair (never exercised on this board before this stage) alongside the already-confirmed PlanetX C/D pair. Confirms both K3 sub-claims: (a) this library's own registration/dispatch path (`on_*_pressed`, `_dispatch`, `run()`) works end-to-end on real hardware for the first time; (b) genuine cooperative multitasking between an input source and a Tier-2 animation — `Buttons.run()` and the display loop run as sibling coroutines under one `asyncio.gather`, and a button-triggered ordinary Tier-1 write (`render_arrow`) correctly cancels whatever Tier-2 animation (scroll or status-line) is in flight, with no lock and no corruption, per CircuitPython asyncio's single-threaded cooperative scheduling. That confirmation used `Buttons` and `on_a_pressed` / `on_b_pressed` / `on_c_pressed` / `on_d_pressed`. The 2026-09-20 API is `OnboardButtons.button_a.on_pressed` and `PlanetXButtonSensor.button_c.on_pressed` (`ButtonPair` removed). `code_stage3.py` matches it and has not been re-run. Alex: "Stage 3 works consistently with the code" — same confirmation bar as Stages 0-2 (general behavioral confirmation, no step-by-step serial-log breakdown reported this round). | `[exp16]` | Alex's live on-device confirmation, 2026-09-13. | 2026-09-13 |
| **`cpfiles.txt` inline-comment manifest bug**: a trailing `# ...` comment on an active `source -> dest` line is not stripped by either `padgettholdings.circuitpythonsync`'s parser or exp16's own `scripts/sync_files_to_board.py` mirror (both `split("->")`-then-`.trim()`) — the comment text becomes part of the destination path, creating a stray malformed file on the board instead of updating the intended one. Recurred twice before diagnosed. **Fixed**: comments moved off active manifest lines in `cpfiles.txt`; `sync_files_to_board.py` hardened to strip trailing comments defensively (the extension itself is unfixed, vendored — the manifest-authoring rule is the real fix). | `[exp16]` `[domain:tooling]` | Reproduced twice on-device (malformed `/code.py # ...` file appeared instead of updating `/code.py`); root-caused by reading both parsers' source. Full mechanism: `concepts/tooling.md` § two similarly-named CircuitPython extensions (2026-09-13 follow-up); coding principle: `universal/CODING_PRINCIPLES.md`. | 2026-09-13 |
| **`Icons` / `Arrows` are statically a bare `type`.** `_build_image_namespace` returns `type` and fills names with `setattr` (`lib/display/core.py`). Pyright does not infer those attributes ([pyright#153](https://github.com/microsoft/pyright/issues/153)), so `Icons.HEART` is not a completable member even inside `core.py`. `Display` methods and `OnboardButtons.button_a` are class-body declarations and are a different case. The live Cursor popup was **not** looked at this session. General mechanism: `concepts/tooling.md` concept *Cursor syntax completion*. | `[exp16]` `[domain:tooling]` | Source of `core.py` 2026-09-21 (`-> type`, `type()` + `setattr`, `ICON_NAMES` 40 / `ARROW_NAMES` 8). Pyright issues #153 and #1531. | 2026-09-21 |
| **Exp16 deploy toolchain exonerated by bracketed repro (S0-S3)**: `scripts/sync_lib_to_board.py` → `scripts/sync_files_to_board.py` → extension "CP Copy Files to Board" (with its auto-reload + full-volume `dot_clean -m` sweep), run from a healthy filesystem, leaves `lib/` fully intact (19/19 files, byte-identical), dir dates real, `code.py` byte-perfect, board running. `dot_clean` deletes exactly the AppleDouble `._*` sidecars — 24 × 4 Ki = 96 Ki, df 223→127 Ki, exact arithmetic accounting. The `circuitpythonsync` 2.2.2 bundle has **no reformat code path** (grep "reformat" = 2 vendored-MIME-db hits only) — nothing in the extension can wipe a CIRCUITPY volume. | `[exp16]` `[domain:tooling]` | Bracketed repro 2026-09-13 (per-step read-only inspection against pre-registered expected states): exp16 `ai-notes/2026-09-13_lib-wipe-repro.md` (S0-S3 sections); extension-source greps same session. | 2026-09-13 |

## Unverified

| Finding | Scope | Why noted | Date |
|---------|-------|-----------|------|
| **Cursor completion popup for `Icons.` / `import display` not observed.** Predictions and the procedure are checks C1–C6 in `ai-persona/ai-notes/circuitpython-syntax-completion/04-experimental-guidelines.md` (may vanish; the predictions are also in `concepts/tooling.md` *Cursor syntax completion*). Promote or refute the evidence row above only after C2 is looked at in the editor. Do not treat a re-read of the note as the check. | `[exp16]` `[domain:tooling]` | Research session 2026-09-21 stopped before invoking the completion list. | 2026-09-21 |
| **`code_stage2.py` extended to 12 steps** (11: rotate 90° while `show_string` scrolls; 12: rotate 180° while `Image.scroll_image` animates), testing the code-level argument that `set_rotation`'s non-cancelling in-place `_LUT` mutation is safe mid-animation (see `lib/display/README.md` § *Rotation during an in-flight Tier 2 animation*, added same session). Both steps serial-self-check that elapsed time is *not* shortened by the rotation (contrast with step 10's cancellation check). Host `py_compile`/pytest clean. **Not yet run on the actual board. Step 11's elapsed-time formula still assumes `WIDTH` columns per glyph; after the spaced font a short elapsed is not by itself a fail.** | `[exp16]` | Design + implementation, Session 31 (2026-09-13); device confirmation pending. Promote to Evidence-Supported once Alex confirms on-device (mirroring steps 1-10's existing row). | 2026-09-13 |
| ~~Disputed-by-source: this row claimed Alex changed the project default to `BRIGHTNESS = 0.05` in Session 16.~~ **Resolved 2026-09-11, moved to Invalidated below** — Alex's authoritative closure: the practical brightness floor is now measured directly (0.01 off / 0.02 lit, see Evidence-Supported row above), which supersedes chasing the historical `0.05`-vs-`0.20` git-value question as material. `core.py`'s actual `BRIGHTNESS` constant remains `0.20` (confirmed by direct read); `0.05` was never a documented default, only an unverified conjecture that had leaked into test-script comments — now corrected there too. Row kept here for history only. | `[exp16]` | Alex, 2026-09-11 (authoritative). | 2026-09-04 (orig.); resolved 2026-09-11 |
| PlanetX C/D **connectors** electrically hit goldfinger P13/P14. **Confirmed 2026-09-11** — see Evidence-Supported row above (K2 fully discharged). Row kept for history only. | `[exp16]` | Firmware maps those edge names to GPIO36/37; physical PlanetX cable now probed and working. | 2026-09-04 |
| ~~Device `import asyncio` works after copying the **bundle** `asyncio` + `adafruit_ticks` onto CIRCUITPY (not CPython stdlib).~~ **Confirmed 2026-09-11** — see Stage 2 Evidence-Supported row above (K1 fully discharged). Row kept for history only. | `[exp16]` `[domain:circuitpython-runtime]` | Adafruit learn: library is not built in; matrix lists `_asyncio` only. | 2026-09-04; confirmed 2026-09-11 |
| No local emulator discharges on-device K1/K2 for stock CP 10.3.0 `bpi_bit_s2`. Wokwi is the only plausible pre-P8 smoke; CircuitPython-on-S2 in Wokwi is unverified. espressif QEMU and Renode have no usable ESP32-S2 story. | `[exp16]` | Web-docs scan 2026-09-07 in local `ai-notes/digests/local-mcu-emulation.md`. Re-check before spending time. P8 stays the on-device gate. | 2026-09-07 |
| **Nezha V2 motor I2C decode is source-backed; nothing on-device yet.** Frame/opcodes in `../../concepts/nezha.md`. `i2c.scan()` for `0x10`, version `0x88`, and the MP-vs-MakeCode disputes (0x5D enum, 0x5E, 0x47 formula) are unverified on BPI-Bit-S2. No driver. | `[exp16]` `[domain:nezha]` | Vendor MakeCode `pxt-nezha2` `main.ts` + MP `Nezha_V2.py`, 2026-09-14. On-device queue: concept how-to-check-status / digest §8. | 2026-09-14 |
| Stock CircuitPython `raspberry_pi_pico2` does not boot in a local emulator (2026-09-07). Host-pytest remains the hardware-free oracle for an RP2350 switch. | `[exp16]` | Web research 2026-09-07 in local `ai-notes/design/rp2350-emulation-macos.md` (projects' own statements, not runs). Revisit if QEMU #3125 lands or an emulator demonstrates CP-on-RP2350. | 2026-09-07 |
| The 2026-09-13 lib-wipe incident's cause is **localized but unproven** to three incident-round-only differentiators (all absent from the clean repro): (a) the **mid-flight cancelled extension copy** (before 10:38 — the board was already "not running properly" with all files still on disk at that point), (b) the **host-side mass rm of `lib/` + `code.py` on the live volume** ("option C" from the parallel chat, ~20 delete+auto-reload events) + eject/replug cycles preceding the re-sync and the second extension copy, and/or (c) **serial terminal attached during the copy** (Alex 11:48: also attached during all prior *successful* days 2026-09-11/12 — so a contributing-condition candidate at most; an active serial REPL suspends auto-reload). Alex's speculations, persisted per request, both unsupported: serial-during-copy corrupting the copy; the board wiping its own FS "noticing an incomplete `code.py`" (no known firmware mechanism auto-reformats on corrupt files). Alex rebooted his Mac after the repro — a post-reboot recurrence weakens host-persistent-state causes. Toolchain itself exonerated (Evidence-Supported row above); no `erase_filesystem()` was executed before the incident (parallel chat prescribed one, Alex chose the rm route; that chat's own transcript self-review confirms). | `[exp16]` | Timeline reconstructed 2026-09-13 from parallel-chat transcript `ea120a3b…jsonl` (user messages 10:38/10:43/10:49 + assistant wipe-menu/self-review), Alex's 11:08 + 11:48 reports, and the repro notes' pre/post states. Recurrence resumption path: `concepts/tooling.md` § CIRCUITPY deploy hygiene. | 2026-09-13 |

## Disputed

| Finding | Scope | Positions | Date |
|---------|-------|-----------|------|
| PlanetX C/D GPIOs on BPI-Bit-S2 goldfinger | `[exp16]` | **Was:** Exp09 `board.IO13`/`IO14` vs official GPIO36/GPIO37 vs “GPIO13/14 = photoresistors”. **2026-09-04 pins.c:** those were two numbering schemes, not two pin pairs — CP `IO13`=`GPIO36`=goldfinger P13; photoresistors = `LUM2`/`LUM1`. Contradiction **resolved** as a naming clash; row kept for history. Remaining: physical PlanetX cable (Unverified). | 2026-09-04 |

## Invalidated

| Finding | Scope | Correction | Date |
|---------|-------|------------|------|
| `board.IO13` / `IO14` on BPI-Bit-S2 CircuitPython mean ESP32 GPIO13/14 (photoresistors). | `[exp16]` | `pins.c` 10.3.0 maps `IO13`→GPIO36, `IO14`→GPIO37. GPIO13 is `LUM2`. | 2026-09-04 |
| The `0.05` figure used throughout early `code.py` test-script comments (as "the project's working floor/default") was a load-bearing constant or documented default. | `[exp16]` | It was an unverified conjecture that leaked into comments; `core.py`'s actual `BRIGHTNESS` constant is `0.20` (confirmed by direct read, Session 19), and the true measured visible floor is 0.01 off / 0.02 lit (Evidence-Supported row above) — neither of which is `0.05`. Alex's authoritative 2026-09-11 statement closed this out; comments fixed in `code.py` and `code_stage0.py`. | 2026-09-11 |
