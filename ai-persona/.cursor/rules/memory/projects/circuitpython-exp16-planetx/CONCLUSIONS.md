# Conclusions — circuitpython-exp16-planetx

Status tiers: `unverified` · `evidence-supported` · `disputed` · `invalidated`.

## Evidence-Supported

| Finding | Scope | Evidence | Date |
|---------|-------|----------|------|
| BPI-Bit-S2 5×5 WS2812 logical (col, row) with origin top-left maps to strip index `row + 20 - column * 5` (column-major, right-to-left physically). **On-device origin now confirmed** (Session 19): `set_pixel(0, 0, WHITE)` visually lit exactly the top-left LED, matching the documented LUT. | `[exp16]` `[cross-experiment]` (from exp09) | Formula + ASCII map in `2026-02_Exp09_…/lib/display_v0.py` and `lib/microbit.py`. On-device confirmation: Alex's live visual observation of Stage-0 `code.py` step 2/5 on UID `0740D10F1BE9`, 2026-09-11. | 2026-09-03; confirmed 2026-09-11 |
| **Stage 0 on-device Tier-1 (sync) rendering path confirmed end-to-end** on UID `0740D10F1BE9`: `import display` succeeds (hardware-import branch, LUT build, NeoPixel buffer alloc); `fill`/`clear_screen`/`set_pixel`/`render_icon` all write real, correctly-shaped pixels; `set_rotation(90)` visibly turns a rendered icon. `set_brightness`'s visual effect was *not* cleanly demonstrated this round (a same-session test-harness bug made it a no-op — see Session 19 log); the underlying setter call itself did not error. | `[exp16]` | Serial capture (`scripts/read_serial_log.py`, all 5 step markers per cycle, zero errors across 2+ cycles) + Alex's live visual confirmation ("consistent with my expectation of the code"), 2026-09-11. | 2026-09-11 |
| **`set_brightness()` practical floor on the onboard NeoPixel (WS2812), UID `0740D10F1BE9`, is authoritative per Alex:** brightness **0.01 and below is visually indistinguishable from off**; **0.02 is the lowest level still visibly lit**. Tested with a full-matrix solid-color fill (not a single pixel), sweeping `0.05 → 0.04 → 0.03 → 0.02 → 0.01 → 0.005 → 0.0` on the same buffer. Likely mechanism (not independently verified): WS2812 PWM duty-cycle/rounding at very low brightness×color-component products. `0.05` is **not** the library's default (that's `lib/display/core.py`'s `BRIGHTNESS = 0.20`, see the Invalidated row below) — it's only ever an ad-hoc dimming-demo value in the test script's step 4. | `[exp16]` | Alex's live visual observation of Stage-0 `code.py` step 6/6, 2026-09-11. | 2026-09-11 |
| Stock CircuitPython **10.3.0** `bpi_bit_s2` firmware includes `_asyncio`, `keypad` / `keypad.Keys` / `keypad.EventQueue`, `rainbowio`, and frozen `neopixel`. User-facing `asyncio` is **not** in the firmware (see `concepts/circuitpython-runtime.md`). | `[exp16]` | 10.3.0 support matrix board cell `docs.circuitpython.org/en/10.3.0/shared-bindings/support_matrix.html` (BPI-Bit-S2); `ports/espressif/mpconfigport.mk` `CIRCUITPY_FULL_BUILD ?= 1`; `py/circuitpy_mpconfig.mk` `CIRCUITPY_KEYPAD ?= $(CIRCUITPY_FULL_BUILD)`; board `mpconfigboard.mk` `FROZEN_MPY_DIRS += …/Adafruit_CircuitPython_NeoPixel`. **On-device `help("modules")` still P8.** | 2026-09-04 |
| CircuitPython `board.IO13` / `IO14` on this firmware **are** goldfinger P13/P14 (GPIO36/GPIO37). `board.NEOPIXEL` = GPIO18. Photoresistors are `board.LUM1`/`LUM2` (GPIO12/13), not `IO13`/`IO14`. | `[exp16]` | `ports/espressif/boards/bpi_bit_s2/pins.c` tag `10.3.0`: `IO13`→`pin_GPIO36` (also `SCK`/`D13`); `IO14`→`pin_GPIO37` (also `MISO`/`D14`); `NEOPIXEL`→`GPIO18`; `BUTTON_A`/`BUTTON_B`→GPIO38/33; `LUM1`/`LUM2`→GPIO12/13. Unifies Exp09 names with BananaPi GPIO numbers (different namespaces). | 2026-09-04 |
| Exp09 5×5 `_FONT` is derived from pitchfork-5x5 (**GPLv3**). | `[exp16]` | Source comment in `2026-02_Exp09_…/lib/microbit.py` (~line 310) + `generate_font.py`. | 2026-09-04 |
| Lancaster DAL `MicroBitFont.cpp` / `pendolino3` is **MIT** (Copyright 2016 BBC; Lancaster by arrangement with the BBC). | `[exp16]` `[domain:fonts]` | File header + https://github.com/lancaster-university/microbit-dal/blob/master/LICENSE (fetched 2026-09-04). Apache-2.0 hypothesis **invalidated**. | 2026-09-04 |
| BananaPi Docs site-wide footer licenses page content (including `bpi_bit_v2_goldfinger.jpg` and `bpi_bit_v2_interface_en.jpg`) as CC BY-SA by BananaPi. Unmodified copies may sit in a public experiment as collection items; ShareAlike does not infect the rest of the tree. Site does not print a CC version. | `[exp16]` | Footer text on https://docs.banana-pi.org/en/BPI-Bit-S2/BananaPi_BPI-Bit-S2. Sidecars: exp16 `Notes/*.jpg.license`. README caption attributes the interface figure. | 2026-09-04 |
| Official 5×5 WS2812 sequential list matches Exp09 (top row `20 15 10 5 0` … bottom `24 19 14 9 4`). | `[exp16]` `[cross-experiment]` | Same BananaPi Bit-S2 page, “5*5 LED Sequential List”. | 2026-09-04 |
| Host visual-fixture tests pin the 5×5 LUT (corners, interior, rotations) to that sequential grid. | `[exp16]` | `tests/test_geometry.py` `_VISUAL_ROT0` (not the wiring formula). On-device LUT still P8. | 2026-09-04 |
| DAL `pendolino3` row-bytes convert to Exp14 column-major via `(row_byte >> (4-c)) & 1`. MIT notice vendored beside the table. | `[exp16]` `[domain:fonts]` | DAL header `!` example + Exp16 `lib/display/font_makecode_5/` + `tests/test_font.py`. | 2026-09-04 |
| Exp09 `Image.GHOST` / `Image.TRIANGLE_LEFT` are **not** the MakeCode grids. Exp16 `GHOST` and `LEFT_TRIANGLE` now follow MakeCode (rounded head; right-triangle with full base). Other icons remain Exp09 ports. | `[exp16]` | Exp09 `lib/microbit.py` vs MakeCode screenshots 2026-09-04. Fixtures: `tests/test_icons_data.py` `MAKECODE_ICON_PATTERNS`. | 2026-09-04 |
| Exp16 `lib/` and `tests/*.py` contain **no** absolute host filesystem paths. Host pytest locates `lib/` via `Path(__file__)` in `tests/conftest.py`. There are no experiment shell scripts. A different checkout’s venv needs **no code edits** — only a local interpreter with pytest. Absolute `/Users/alex/…` paths are prose (example in `tests/README.md`, locks in `ai-notes/NOTES.md`). Shared workspace CircuitPythonSync is **outside** this experiment. | `[exp16]` | Grep 2026-09-04: no `/Users/`, `/Volumes/`, `PythonVEs` under `lib/` or `tests/*.py`. | 2026-09-04 |
| **This plugged-in unit** (UID `0740D10F1BE9`) ran CircuitPython **10.3.0** `bpi_bit_s2` as of 2026-09-11 Path A UF2 copy (was 10.0.3). Other BPI-Bit-S2s in the fleet are **not** assumed to match; re-read `boot_out.txt` per board. | `[exp16]` | `/Volumes/CIRCUITPY/boot_out.txt` after Path A: `Adafruit CircuitPython 10.3.0 on 2026-08-31; BPI-Bit-S2 with ESP32S2`; USB `303A:80E6`. Prior 10.0.3 on same UID. Alex: multiple boards, do not share version. | 2026-09-11 |
| Path A (CP `.uf2` onto `BITS2BOOT` with TinyUF2 already **0.35.0**) upgraded this unit 10.0.3 → 10.3.0 and **kept** CIRCUITPY (`code.py`, `lib/`, `sd/`, `settings.toml`); prior LED-icon sketch still ran. | `[exp16]` `[domain:tooling]` | Alex report + `boot_out.txt` 10.3.0 same UID; listing still had those files. TinyUF2 not rewritten. | 2026-09-11 |
| UID `0740D10F1BE9` CIRCUITPY FAT volume is **983040 bytes** (~941 KiB `df`; ~745 KiB free with current sketch). That **is** TinyUF2 `ffat` 960K — **same size** on dual-OTA and no-OTA 4MB tables. TinyUF2 `Flash Size: 0x002C0000` is **`ota_0`** (~2.75 MiB), not CIRCUITPY. Prior chat claim “pre-0.33 CIRCUITPY would be larger” is **invalidated**. | `[exp16]` `[domain:tooling]` | TinyUF2 0.32/0.35 CSVs (`ffat` 960K both); `diskutil` 2026-09-11; Adafruit Learn CP10 4MB page. Tables: `../../concepts/tooling-4mb-partitions.md`. | 2026-09-11 |
| BPI-Bit-S2 TinyUF2 mass-storage volume is **`BITS2BOOT`**. Enter it with tap **RESET** then tap **BOOT** within ~1 s (matrix purple), not Adafruit’s generic double-RESET as the first try. ROM download mode (esptool) remains hold **BOOT**, tap **RESET**, release **BOOT**. BananaPi Bit-S2 pages that say `esptool --chip esp32s3` are a **wrong chip** — use `esp32s2`. | `[exp16]` `[domain:tooling]` | Board page + BPI-Steam `update_circuitpython.html` (BITS2BOOT / RST then BOOT); circuitpython.org ROM-mode recipe; TinyUF2 0.35.0 + CP 10.3.0 URLs HTTP 200 on 2026-09-11. Full recipe: `../../concepts/tooling.md`. | 2026-09-11 |
| **No inter-character gap in scrolled/held text; some glyphs fill all 5 columns of the DAL `pendolino3` font's fixed advance width, so adjacent full-width glyphs visually merge (not yet fixed).** Alex's hypothesis (Session 22, watching Stage 2 steps 6/7 render "STAGE2"/"42"): some characters look 4 columns wide with visible separation, others look 5 columns wide with none. Mechanically confirmed by reading `font_makecode_5/glyphs.py`'s `_COLUMN_MAJOR` table + decoding the bytes: **every glyph is stored as exactly `WIDTH` (5) column bytes** (fixed advance width, per `glyph_columns`'s own contract) — there is no variable-width metadata. What actually varies per-glyph is whether that glyph's own bitmap leaves a column blank: `S`/`A`/`E`/`2`/`K` all have a blank rightmost column (decoded: `[18,21,21,9,0]`/`[30,5,5,30,0]`/`[31,21,21,17,0]`/`[25,21,21,18,0]`/`[31,4,10,17,0]`), which acts as a self-contained visual margin; `T`/`G`/`4` use all 5 columns with a non-blank rightmost byte (`[1,1,31,1,1]`/`[14,17,17,21,12]`/`[12,10,9,31,8]`), leaving no margin. Separately confirmed in `core.py`: `_GlyphColumnFeeder`/`show_string`'s scroll loop (~955-995) and the short-text centered-hold path never insert an explicit inter-glyph spacer column — spacing comes *entirely* from whichever blank columns happen to be baked into individual glyph bitmaps. So `"STAGE2"` merges at `G`→`E` (G's non-blank right column touches E's left column) and `"42"` merges at `4`→`2` (same mechanism) — both cases Alex flagged. **Open question, resolved 2026-09-11**: checked against the actual Lancaster `microbit-dal` source (`MicroBitDisplay.cpp`'s `updateScrollText`) — genuine upstream hardware advances the scroll position by `width + MICROBIT_DISPLAY_SPACING` (spacing constant = 1) before pasting the next glyph, i.e. it unconditionally reserves one blank pixel-column between every character pair regardless of either glyph's own bitmap. This port's missing spacer is a **genuine gap relative to upstream**, not an inherited property of the vendored font data. Full 95-glyph survey: 62 already margined (blank rightmost column), 32 full-width, 1 all-blank (space). **Not fixed**: a design proposal now exists (analysis-only, not implemented) — exp16 `ai-notes/design/font-inter-glyph-spacing.md` — owned entirely by `_GlyphColumnFeeder` via a `_spacer_pending` flag (no ring-buffer resize, no new public parameter on `show_string`/`show_number`). **Revised 2026-09-12** on Alex's follow-up: recommendation flipped from an unconditional spacer (matches DAL bit-for-bit, § 6a in that doc) to a *conditional* one, set only when the just-finished glyph's own last column is non-blank — quantified as strictly cheaper and mostly-uniform (85% of glyph transitions get an exact 1-column gap; full 95-glyph trailing-blank-count survey in that doc's § 3b), at the cost of a deliberate, scoped departure from DAL's literal timing. That document also scrutinizes and rejects Alex's own sketched alternative (resizing the scroll ring to `WIDTH+1`) as solving the wrong layer. **§ 6b (2026-09-12, third round):** the conditional design reads `self._cols[-1]`, which would raise `IndexError` on an empty glyph — proven unreachable under the current font contract (`font_makecode_5.glyph_columns` always returns exactly `WIDTH` bytes, already pinned by `tests/test_font.py::test_unknown_is_blank`), guard kept anyway as insurance + consistency with this class's existing defensive style; surfaces that `_GlyphColumnFeeder` has zero *direct* host-test coverage (same hardware-import gate that excludes `Image`, per the icon-storage doc), not fixed here. **§ 6c (2026-09-12, fourth round):** generalized to trimming *all* redundant blank margin (both sides, every glyph), per Alex's request, with runtime cost and code complexity quantified rather than assumed — O(WIDTH) trim scan vs. § 6's O(1) check; equal column-count to § 6 on plain letter/digit strings, real savings (7-19% in the two multi-word examples measured) only on strings with spaces/heavy punctuation; makes the previously-hypothetical all-blank-glyph case fire on every space in real text with no direct test coverage; demonstrated word-gap-collapse legibility risk (a space costs the same 1 column as an ordinary letter gap). Not recommended as the default; kept as a documented, scoped-follow-up option. **§ 6d (2026-09-12, fifth round):** Alex proposed precomputing `(lead, trail)` into the font table as the fix for both the runtime-scan cost and the whitespace bug. Scrutinized as two separable ingredients: precomputing alone fixes only the runtime cost (still traps every space into the same broken empty-glyph path unless the geometrically-honest value is overridden); the actual legibility fix is an independent "never trim a fully-blank glyph" rule. Recomputed the column-count savings with that rule applied: they collapse from the 7-19% originally reported to 0-5% on ordinary natural-language text (`"HELLO WORLD"` goes from a 7% saving to *zero*), because most of the original saving was the bug itself; large savings (20-60%) persist only for punctuation-/narrow-letter-dense strings. § 6 remains the recommended default; the corrected/precomputed design (not the naive on-demand one) is the one to build later if usage data ever shows this project's scrolled text is numeral/punctuation-heavy enough to justify it. **§ 6e (2026-09-12, sixth round):** Alex proposed redefining `(lead, trail)` from "measured blank margin" to "authored occupied span in a spaced sequence" — confirmed correct, and it resolves § 6d's own "gotcha" at the definition level (a fully-blank glyph's span is a free design choice from the start, not a wrong measurement patched after the fact); motivated a rename (`skip_lead`/`skip_trail`) and cleanly posed Alex's "maybe 4 instead of 5?" tuning question, answered with recomputed numbers (~1 column saved per space if narrowed to 4; default recommendation stays `(0,0)`/full-width, matching § 6's existing space handling). No change to the overall verdict (§ 6 still default). **§ 6f (2026-09-12, seventh round):** Alex gave a concrete counter-example, `"27.3"` (a realistic decimal sensor-readout string) — proved § 6 leaves the `.`→`3` gap at 3 columns, identical to today's pre-fix behavior, because § 6 only ever adds a missing spacer and never trims excess native margin; not a hypothetical concern, a demonstrated one. Introduced and analyzed a new candidate (§ 6f): cap only a glyph's own trailing margin to at most 1 column, no leading-margin data, no font-table schema change — cheaper than § 6d/G, bounds worst-case inter-glyph gap to 2 (proven exhaustively across the font), down from § 6/today's worst case of 4. Column-count savings sit between § 6 and § 6d. **This is now the document's strongest recommendation if any spacing normalization beyond bare § 6 is wanted** — presented as the leading candidate, not a final decision; three real options remain (§ 6, § 6f, § 6d/G), Alex's call. **§ 10 (2026-09-12, eighth round):** Alex asked for a scope overview plus whether a parallel-implementation/single-call-site-swap rollout is workable. Confirmed against the code: `_GlyphColumnFeeder` has exactly one external reference (`core.py:977`) and a 2-method interface — recommended shape is a new, separate module + isolated tests + a 2-line `core.py` cutover, orthogonal to which mechanism (§ 6/§ 6f/§ 6d) is chosen; still a plan, no code added. **§ 11 (2026-09-12, ninth round):** Alex leaned toward **§ 6d+§ 6e** (full trim, not § 6f) and asked for a final gotcha check. One real simplification found: the precomputed `(skip_lead, skip_trail)` table needs no separate generation script/build step — a module-import-time computed loop over the existing `_COLUMN_MAJOR` bytes suffices (same pattern as this file's own `_build_image_namespace`), validated against this session's survey data (`_glyph_skip(" ")==(0,0)`, `.`→`(1,3)`, `7`→`(0,0)`, unknown-char→`(0,0)`, all matching). Corrects § 5/§ 10's earlier "build tooling" framing. Everything else re-checked (naming: use § 6e's `_glyph_skip` not § 6d's `_glyph_margins`; all-blank branch stays a branch, not a formula; all 4 glyph-adjacency collision cases hold; § 6b's guard stays unreachable, now for a slightly stronger reason; § 6e's "how wide should space be" question stays open) with **no blocking correctness gaps found**. **§ 12 (2026-09-12, tenth round):** Alex proposed interleaving `(lead, trail)` into each glyph's own record (nibble-packed, `1+WIDTH` bytes/glyph) rather than § 11's separate skip table, and distinguished a (currently nonexistent — checked, not assumed) dev-machine build step from what ships on-device. Validated nibble-packing as a strict improvement (95B vs. § 11's 190B, sufficient for both the 5×5 and 8×8 target geometries). Key finding: the "when computed" (import-time vs. build-time) and "how laid out" (separate vs. interleaved) axes interact — interleaving's drift-safety benefit only matters once values are build-time-baked, since import-time-computed values can't drift from `_COLUMN_MAJOR` regardless of layout. `glyph_columns`'s external contract confirmed fully preservable either way (no test/caller reaches the raw table directly). Presented as a genuine three-way scope fork, not resolved: (a) § 11 import-time/separate, cheapest, no new tooling; (b) Alex's build-time/interleaved, zero on-device cost + eliminates drift risk, but requires creating new build tooling (real standing value for the declared 8×8 target); (c) build-time/separate hybrid. **§ 12a (2026-09-12, eleventh round, revises § 12):** Alex decided the DAL-conversion should be preserved as a real, committed, re-runnable, never-deployed script (independent of the skip-data question) and pushed back on § 12's drift-risk-centric framing with a broader principle — see `universal/CODING_PRINCIPLES.md § Separate build-time policy computation from runtime business logic`. Net: **the "when computed" axis is now resolved toward build-time-baked** (no longer a symmetric fork — the script is happening anyway, and the principle favors folding skip-data into it over § 11's import-time loop). The drift-risk argument for interleaved-over-separate was retracted as over-weighted once checked (`git log --follow -- glyphs.py`: exactly one commit ever, confirming hand-edits to this kind of artifact are in practice rare here) — interleaved-vs-separate is now a near-coin-flip style question, not a correctness one. **§ 12b (2026-09-12, twelfth round, consolidates §§ 11-12a):** Alex asked whether a parallel-implementation rollout (new function + new table, old ones untouched — mirroring § 10's already-established strategy one layer down) resolves § 12's "editing `glyph_columns` in place" cost; confirmed yes, fully — retracted that claim, closing the last operational-cost gap between interleaved and separate layouts. Confirmed his lean toward build-time-baked/interleaved/nibble-packed as a real font-conversion pipeline (not a small add-on); confirmed folding `(lead, trail)` into the build script using either explicit objects or implicit tuples internally. Wrote a consolidated cold-AI digest in the design doc: current direction + a register of every dismissed alternative (drift-risk-as-deciding-factor, § 11's import-time loop, in-place editing, combined-return signature) each with its dismissal reason, so future re-derivation isn't needed if these resurface. **Decision (2026-09-12, same day):** Alex explicitly chose **interleaved** — one combined array, each glyph's record `[skip_byte, col0, col1, col2, col3, col4]` — closing the layout question §§ 12/12a/12b had already established as style-only (no cost/correctness asymmetry) with a firm choice rather than a lean. **§ 13 (2026-09-12, same day, accessor return shape):** Alex questioned whether the new accessor should return the already-trimmed ink slice directly (a single variable-length `bytes`) rather than a `(bytes, lead, trail)` triple. Confirmed `bytes` slicing always copies (a trimmed slice is never costlier); verified against the actual `show_string` source that the fit-on-screen centering block (`core.py:969-974`) needs zero changes (depends only on `len(fit_buf)` + generic byte access) — while precisely noting the `fit_buf`-building loop (`core.py:956-961`) needs to change regardless of accessor shape (spacer insertion, part of § 6d's algorithm itself). Found `core.py`'s `_glyph_columns` wrapper (L302-313) is an existing single choke point already shared by the scroll feeder *and* the fit-on-screen loop. Recommended adopting the trimmed-slice shape (also simplifies § 12b's record meaning to `[length, ink...]`, same total size) — no downside found. **§ 14 (2026-09-12, same day, full-adoption consolidation):** Alex asked to adopt § 13 (and §§ 10-12b) fully into the design. Consolidating end-to-end caught two real gaps: (1) a bare `bytes` return can't distinguish a blank pass-through glyph from a non-blank full-width glyph (both return exactly `WIDTH` bytes) — fixed by returning `(bytes, needs_spacer: bool)`; (2) "spacer after every non-blank glyph" emits a trailing spacer that's harmless for scrolling but breaks fit-on-screen centering — fixed by prepend-based spacer semantics (before glyph N if glyph N-1 was non-blank; no trailing spacer either path). Wrote the single consolidated, adopted spec (algorithm + storage + accessor + rollout) and corrected a stale § 9 bullet that still described § 6d/§ 6e as undecided after round 8 had already resolved it. **Round 15 (2026-09-12)**: Alex asked what `needs_spacer` disambiguates (blank pass-through glyph vs. ink-bearing glyph of the same trimmed byte-length — answering surfaced that this collision is common, not rare: 49/94 ink-bearing glyphs already trim to the same 4-column length now chosen for space) and decided § 6e's previously-open space-width knob: **space is 4 columns wide on the current `WIDTH=5` font**, chosen via a per-font heuristic (match the font's own common narrow-letter width) rather than a `WIDTH`-derived formula; a future font (8×8 target) re-applies the same heuristic fresh (Alex's own estimate: 6-7, not a fixed number). Also did a requested pragmatic consistency/structure pass over the whole design doc: top-of-file cold-AI orientation block, per-section `[FINAL]`/`[historical]`/`[superseded]` status tags, fixed stale storage-shape estimates in §§ 5/10a, synced `ai-notes/INDEX.md`/`NOTES.md`. **Round 16**: Alex confirmed § 14a Gap 2's no-trailing-spacer fix applies to the scroll path too (previously a recommendation, not confirmed) — now settled, one rule for both render paths. | `[exp16]` `[domain:fonts]` | Alex's live visual observation of Stage 2 steps 6/7, 2026-09-11 (`show_string('STAGE2')` scrolling, `show_number(42)`); mechanically verified same session via a host-side decode of `font_makecode_5/glyphs.py`'s actual bytes for `S/T/A/G/E/2/K` + a read of `core.py`'s `_GlyphColumnFeeder`/scroll-loop source confirming no spacer column exists anywhere in the render path. | 2026-09-11 |
| **`show_number(n: int, ...)`'s type hint overstates its own contract — implementation already accepts any `str()`-able value without crashing, but its output quality for float/bool/nan is undocumented and unconsidered (not yet fixed).** Surfaced 2026-09-12 while analyzing the font-spacing doc's `"27.3"` example (a decimal sensor readout). `show_string` already defensively `str()`-coerces its own input, so `show_number` cannot crash on a float, `bool` (an `int` subclass — `show_number(True)` already type-checks *today*, producing `"True"` not `"1"`), big int, or `nan`/`inf` — the real gap is display quality (host-CPython `str(1/3)` → 18-char `"0.3333333333333333"`; scientific notation for very large/small floats) and doc-truth, not correctness. Grounded CircuitPython's float precision against 3 independent GitHub issues (not asserted from memory): default 30-bit float (`MICROPY_OBJ_REPR_C`, narrower than IEEE754 single precision) — confirmed for RP2040/SAMD21-family ports, **not directly confirmed for this project's ESP32-S2/RP2350 targets** (flagged unverified, needs on-device check); host-CPython `str(float)` testing does not reliably predict on-device output. Design doc with routes A-D (widen type hint/doc truth; bounded `decimals` param; guard nan/inf/bool; no-op) — exp16 `ai-notes/design/show-number-numeric-types.md`. **Decided 2026-09-12 (same day, round 2):** Alex approved A+B; **explicitly rejected C** — nan/inf may be legitimate output-domain values and are valuable for debugging even as bugs; bool-as-text is over-permissive but not unsafe — no guards added anywhere, existing fully-permissive `str()`-based acceptance preserved. Alex also requested a `scientific: bool | None = None` companion to `decimals`; approved after testing surfaced two real, now-documented interaction gotchas: (1) supplying either `decimals` or `scientific` formats a `bool` as its numeric value (`"1.00"`), not `"True"`/`"False"`; (2) both convert their operand through `float` first, so very large `int`s (beyond ~2^53) can silently lose exact-digit precision versus plain `str(n)`. **Revised same day (round 3):** Alex reconsidered the bool position specifically (nan/inf reasoning untouched) — `decimals`/`scientific` should be ignored entirely for `int` inputs (structurally removes gotcha 2, not just documents it: `int` never reaches the float-converting format spec), and `bool` should be explicitly rejected (`TypeError`) rather than tolerated, since a type check is needed anyway to route int-vs-float — `str` is excluded for free by the same check. +2 lines, O(1)/nanosecond added cost, net correctness improvement (removes gotcha 2 outright; replaces gotcha 1's silent surprise with an explicit error). Not implemented; analysis-only. | `[exp16]` `[domain:circuitpython-runtime]` | Code read (`core.py` `show_number` L1007-1021, `show_string` L930-934 confirming the defensive `str()` coercion); `adafruit/circuitpython` GitHub issues #9237, #5802, #583 + `micropython/micropython` #15236 (fetched 2026-09-12). | 2026-09-12 |
| **Color constants in `lib/display/_constants.py` are perceptually off and need tuning (not yet done).** `YELLOW = (255, 150, 0)` renders on-device as a warm yellow with a visible orange tinge (Alex: "the warmth is fine, but the tinge isn't"); `ORANGE = (255, 40, 0)` renders as **plain red**, indistinguishable from `display.colorwheel(0)` (an independent, library-external "true red" baseline: `0xFF0000`) — its green channel (40) is too low to read as orange against the WS2812's actual emission. Both observations are consistent with the raw RGB values themselves (orange's green channel is only 16% of max). **Not fixed this session — tracked as a TODO for a later color-tuning pass** (iterate the RGB constants against how they actually look on this specific WS2812/diffuser, not just the numeric values), see `SESSION_LOG.md § Open Questions`. | `[exp16]` | Alex's live visual observation of Stage-1 `code.py` steps 4/6 (`Icons.HAPPY` in `YELLOW`), 5/6 (ring pattern in `ORANGE`), and 6/6 (`colorwheel(0)` sweep, same cycle) confirming ORANGE vs. colorwheel-red is a no-visible-difference comparison, 2026-09-11; cross-checked against `lib/display/_constants.py` lines 34-35. | 2026-09-11 |
| **Stage 1 (broader Tier 1) fully confirmed on-device**, UID `0740D10F1BE9`: all 6 steps (`render_pattern`, `render_arrow`, `get_pixel` self-check, `HAPPY` icon at 4 rotations, `create_image`+`render_icon`, `colorwheel` sweep) match expectations per Alex's live visual pass ("what I would expect based on the code," aside from the separately-tracked color-tuning TODO above). Serial self-check for step 3 independently confirms the read-back path: `set_pixel(2,2,MAGENTA) -> get_pixel(2,2) = (255, 0, 20) [OK]`. | `[exp16]` | Alex's live visual confirmation, 2026-09-11 + autonomous serial capture (`scripts/read_serial_log.py`, read-only, 28s window) same session: cycle 87 complete with all 6 step markers, zero drops/errors, `get_pixel` self-check printed `[OK]`. | 2026-09-11 |
| **Stage 2 (Tier 2 async, display-only) fully confirmed on-device — K1 discharged.** All 10 steps of `code.py` ran on UID `0740D10F1BE9`: bundled `asyncio` runs at all (step 1, the actual first exercise of K1); the async `show_leds`/`show_icon`/`show_arrow`/`show_string`/`show_number` wrappers render correctly (steps 2-7); `Image.show_image`'s 5-offset window sweep (aligned/straddling/negative/overhang) and `Image.scroll_image`'s full-width animation both work (steps 8-9); the cancellation-token contract (`Display._acquire`/`_is_cancelled`) correctly cuts a slow scroll short when an ordinary Tier-1 write fires concurrently (step 10). Alex: "the LED Matrix behaves in accordance with what I would expect based on the code" — matches the Stage 0/1 confirmation bar (no serial-log capture reported this round, visual confirmation only). Frozen as sibling `code_stage2.py`. | `[exp16]` | Alex's live visual confirmation, 2026-09-11. | 2026-09-11 |
| **K2 fully discharged.** PlanetX C/D connectors (via Nezha Pro/v2 breakout on `J3`) electrically reach goldfinger P13/P14 → `board.IO13`/`IO14` → GPIO36/GPIO37, and physically pressing them fires `keypad.Keys` events on UID `0740D10F1BE9`: `key_number=0` (IO13) = **C**, `key_number=1` (IO14) = **D**, matching construction order `keypad.Keys((board.IO13, board.IO14), value_when_pressed=False, pull=True)`. Both buttons fire repeatedly and independently, no cross-talk observed. Supersedes the Unverified row below (kept for history). | `[exp16]` | Live serial capture, Alex's own `Serial Monitor (miniterm)` task, 2026-09-11: `Color snake ..PlanetX button C pressed (key_number=0)` / `...button D pressed (key_number=1)`, repeated presses of both. | 2026-09-11 |

## Unverified

| Finding | Scope | Why noted | Date |
|---------|-------|-----------|------|
| ~~Disputed-by-source: this row claimed Alex changed the project default to `BRIGHTNESS = 0.05` in Session 16.~~ **Resolved 2026-09-11, moved to Invalidated below** — Alex's authoritative closure: the practical brightness floor is now measured directly (0.01 off / 0.02 lit, see Evidence-Supported row above), which supersedes chasing the historical `0.05`-vs-`0.20` git-value question as material. `core.py`'s actual `BRIGHTNESS` constant remains `0.20` (confirmed by direct read); `0.05` was never a documented default, only an unverified conjecture that had leaked into test-script comments — now corrected there too. Row kept here for history only. | `[exp16]` | Alex, 2026-09-11 (authoritative). | 2026-09-04 (orig.); resolved 2026-09-11 |
| PlanetX C/D **connectors** electrically hit goldfinger P13/P14. **Confirmed 2026-09-11** — see Evidence-Supported row above (K2 fully discharged). Row kept for history only. | `[exp16]` | Firmware maps those edge names to GPIO36/37; physical PlanetX cable now probed and working. | 2026-09-04 |
| ~~Device `import asyncio` works after copying the **bundle** `asyncio` + `adafruit_ticks` onto CIRCUITPY (not CPython stdlib).~~ **Confirmed 2026-09-11** — see Stage 2 Evidence-Supported row above (K1 fully discharged). Row kept for history only. | `[exp16]` `[domain:circuitpython-runtime]` | Adafruit learn: library is not built in; matrix lists `_asyncio` only. | 2026-09-04; confirmed 2026-09-11 |
| No local emulator discharges on-device K1/K2 for stock CP 10.3.0 `bpi_bit_s2`. Wokwi is the only plausible pre-P8 smoke; CircuitPython-on-S2 in Wokwi is unverified. espressif QEMU and Renode have no usable ESP32-S2 story. | `[exp16]` | Web-docs scan 2026-09-07 in local `ai-notes/digests/local-mcu-emulation.md`. Re-check before spending time. P8 stays the on-device gate. | 2026-09-07 |
| Stock CircuitPython `raspberry_pi_pico2` does not boot in a local emulator (2026-09-07). Host-pytest remains the hardware-free oracle for an RP2350 switch. | `[exp16]` | Web research 2026-09-07 in local `ai-notes/design/rp2350-emulation-macos.md` (projects' own statements, not runs). Revisit if QEMU #3125 lands or an emulator demonstrates CP-on-RP2350. | 2026-09-07 |

## Disputed

| Finding | Scope | Positions | Date |
|---------|-------|-----------|------|
| PlanetX C/D GPIOs on BPI-Bit-S2 goldfinger | `[exp16]` | **Was:** Exp09 `board.IO13`/`IO14` vs official GPIO36/GPIO37 vs “GPIO13/14 = photoresistors”. **2026-09-04 pins.c:** those were two numbering schemes, not two pin pairs — CP `IO13`=`GPIO36`=goldfinger P13; photoresistors = `LUM2`/`LUM1`. Contradiction **resolved** as a naming clash; row kept for history. Remaining: physical PlanetX cable (Unverified). | 2026-09-04 |

## Invalidated

| Finding | Scope | Correction | Date |
|---------|-------|------------|------|
| `board.IO13` / `IO14` on BPI-Bit-S2 CircuitPython mean ESP32 GPIO13/14 (photoresistors). | `[exp16]` | `pins.c` 10.3.0 maps `IO13`→GPIO36, `IO14`→GPIO37. GPIO13 is `LUM2`. | 2026-09-04 |
| The `0.05` figure used throughout early `code.py` test-script comments (as "the project's working floor/default") was a load-bearing constant or documented default. | `[exp16]` | It was an unverified conjecture that leaked into comments; `core.py`'s actual `BRIGHTNESS` constant is `0.20` (confirmed by direct read, Session 19), and the true measured visible floor is 0.01 off / 0.02 lit (Evidence-Supported row above) — neither of which is `0.05`. Alex's authoritative 2026-09-11 statement closed this out; comments fixed in `code.py` and `code_stage0.py`. | 2026-09-11 |
