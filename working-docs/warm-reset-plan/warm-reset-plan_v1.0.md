# Warm-Reset Execution Plan — v1.0 (SIGN-OFF CANDIDATE)

**Status**: **v1.0 — sign-off candidate** (the `_META.md` planning loop converged at `v0.5`, iteration 5; this is a `cp` of that converged version, promoted to major version for Alex's go/no-go). **Authored**: 2026-06-14. **Consumer**: **Alex** (go/no-go gate) + AI at execution. **Loop governance**: `_META.md` (same folder). **Provenance**: derived through `v0.0`→`v0.5` (see `notes_v0.1.md`…`notes_v0.5.md` for the full revision trail; `experiment-log.md` for sub-agent technique tuning). Convergence (T3→T2→T2→T2→T1; adequacy-unknowns 3→2→2→1→0) recorded in `notes_v0.5.md`.
**Companion artifact**: `risk-register.md` — the irreducible a-priori judgment calls (priority escalation set for Alex).
**Post-convergence revisions (Alex, pre-go-ahead; `v0.5` preserves the as-converged snapshot)**: (R-rev1) mandate reframed as an *adaptable template*, not a source of truth. (R-rev2, 2026-06-14) **single unified persona memory** — one memory in one cross-workspace location (NOT federated); central holds high-level (per-project digests, rough status/resumption, entry-points linking into project folders, generalizable + boundary learnings, modus-operandi feedback, coding guidelines, style prefs); project folders hold technical artifacts (execution detail, sketches, thought experiments) linked from central, never duplicated. R-rev2 **decides D7 (unified, not federated) and D6** (one location = central tree stays in the CircuitPython workspace's `.cursor/rules/`, symlinked into the other workspaces; path confirmed by Alex 2026-06-14). (R-rev3, 2026-06-14) Alex agreed with the plan's recommendations on **all remaining decisions** — D1 (per-experiment + family), D2 (auto-promote, sign-off for universal), D3 (principle + R-7 fix), D4 (orthogonal scope models), D5 (strip stale tiers), D8 (graduated concept-graph) + risk fixes R-8 (claims-coverage no-loss check) and R-9 (concept reshaping = approved anti-goal exception, two sections). **D1–D8 now all resolved**; only the R-6 reachability test and the "warm reset" trigger remain.
**Execution gate (unchanged, hard)**: this plan is NOT executed until Alex says the exact phrase "warm reset" + gives go-ahead (mandate strict-phrase rule). Resolving D1–D8 (§3) is a separate prerequisite to execution.
**Template from prior execution (a starting point, NOT a rigid source of truth)**: `/Users/alex/Development/VsCode/CircuitPython/.cursor/rules/mandates/multi-project.md` (the mandate) is the design *template* this plan operationalizes. It encodes choices made before the persona's current state, so treat it as an **adaptable prior, not an authority that "wins by default"**: where the persona's evolved memory structure or high-level memorized information warrants, the target structure is adapted. That fit-assessment is itself an execution activity (Phase 0) — the template is updated against current evidence, not assumed correct. Adaptations: low-stakes ones are made transparently and logged (§3a DV1/DV2); high-stakes / structural ones escalate to Alex (§3a DV3; §6 authority map).

---

## 1. Target (FIXED — changes escalate to Alex)

Reorganize the flat CircuitPython memory at `.cursor/rules/memory/` into a **single, unified persona memory** — one memory, one location, shared across all projects (exp14, exp15, Bamboo-Lamp, future) — so that:
- **One persona, one memory home.** All of the persona's *memory* lives in one central location reachable from every project workspace, **not** federated into per-project repos (Alex 2026-06-14, supersedes the earlier federation idea).
- **Central memory = high-level only**: per-project **digests** (what was done + rough status + where to resume), **entry points** that link into the project folders for deeper reading, **generalizable learnings**, **modus-operandi feedback**, **coding guidelines / style preferences**, and **boundary concepts** (insights sitting between project-specific and generally-applicable).
- **Project folders = technical artifacts** (execution details, sketches, thought experiments) — these stay in the project folder and are **linked** from central memory, never duplicated into it.
- per-project *digests* are isolated within central (no cross-contamination — the M1 failure mode; *M1 = the mandate's active-project-identifier: match the most-recently-edited file path against `projects/_INDEX.md` path-globs to pick the active project; its failure mode is writing into the wrong project's memory*); the heavy technical detail lives in each project folder.
- cross-project patterns can be detected and promoted/demoted deliberately.
- **all existing content is preserved** — warm reset is a *structural move + tag* (+ hoist of high-level content to central, link-down to project artifacts), never a content rewrite or prune.

Outcome test (not artifact): a cold AI starting a session in any project reads the ONE central memory, gets that project's high-level digest + entry points, follows links into the project folder for technical detail, and cannot accidentally write another project's memory.

## 2. Constraints (FIXED — invariants that must hold)

- **C1 No content loss.** Every pre-reset entry survives, moved and/or tagged. Verifiable by diffing the pre-reset snapshot against the post-reset content (set of entries unchanged; only location/tags differ).
- **C2 Snapshot-before-touch.** Copy all of `memory/` to `.cursor/rules/memory-pre-warm-reset-<YYYYMMDD-HHMMSS>/` before the first mutation. Rollback path.
- **C3 Always-injected rules stay valid.** The `.mdc` files (`00-memory-system`, `02-domain-structure`, `03-memory-update-triggers`) reference memory paths; after the move, no rule may point at a dead path.
- **C4 One central memory, reachable from every project workspace.** The *entire* persona memory (not just the behavioral files) lives in a single location loadable when working any project — CircuitPython exp14/exp15, Bamboo-Lamp, and future ones — not federated per-repo (Alex 2026-06-14).
- **C5 Cold-AI retrieval intact.** Session-start active-retrieval (read living summary + relevant project memory) must still function after the layout change.
- **C6 No execution without the trigger.** Alex's exact "warm reset" phrase + go-ahead precedes any file mutation.
- **C7 Seed only on evidence; mark the new structure provisional.** Create only structures with content in evidence at move time — no empty domain folders, no per-concept files below volume threshold, no speculative `crossref` categories (guidelines §2 seed-on-evidence, §9 counter-bloat). Rationale anchored in the actual content: `TECHNICAL.md` has only two populated sections (CircuitPython memory-management, fonts) atop all-empty schema tables; `CONCLUSIONS.md` has one finding; domains power/i2c/deep-sleep/fuel-gauge/led-driving have zero content today. Tag the whole new layout `provisional (as of <move date>)` with a watch-for block that is itself cold-AI-testable (per cold-ai-paradigm §7 — a marker without confirm/refute + a trigger is decoration): **confirm** = a real post-reset query resolves one-hop (index→domain→concept) with no speculative search; **refute** = a query needs scanning multiple files / speculative search, or a new finding has no deterministic placement-gate home; **trigger** = re-evaluate after ~N real memory additions land post-reset (or at the next maintenance session). Remove the `provisional` marker once ~N additions land with no refute signal (guidelines §2).
- **C8 Central is self-sufficient at the high level; project links are for further reading only.** Every central entry must be usable/decodable without opening a project folder (cold-AI self-containment): the digest, status, generalizable learning, or boundary concept carries its own load-bearing content; links into project folders (`2026-04_Exp14_*/…`, `Bamboo-Lamp/…`) provide *deeper technical detail*, never the context needed to decode the central entry. (Project folders are stable git repos — acceptable link targets, unlike the ephemeral persona dirs guidelines §3.2 forbids depending on.) Corollary: do NOT copy technical execution detail into central; reference it.

## 3. Decisions (ALL RESOLVED by Alex 2026-06-14 — execution prerequisites cleared)

**All D1–D8 are now DECIDED.** D6/D7 were settled first (one unified memory, not federated; central tree in the CircuitPython workspace's `.cursor/rules/`, symlinked into other workspaces). D1, D2, D4, D5, D8 + the risk-register fixes (R-7, R-8, R-9) were then resolved by Alex agreeing with the plan's recommendations. The only remaining execution prerequisites are the **R-6 reachability smoke-test** (an execution-time pre-check) and **Alex's "warm reset" trigger + go-ahead** (the hard gate). Each decision below is marked with its resolution. (**OQ4/DN-MP-1** — the mandate's new-project-spin-up heuristic — is intentionally *not* in this list: the mandate tracks it as a deferred design note that is **non-blocking** for warm reset ("blocks only on OQ1–OQ3"). Its heuristic lands in `projects/_INDEX.md` header and is revised after 3–5 projects of evidence, guidelines §6.)

- **D1 (OQ1) Project granularity. — DECIDED = (a)+(d).** Per-experiment projects under a `circuitpython` family, with Bamboo-Lamp as a separate family. Rationale: matches current reality (exp14/15 active; Bamboo-Lamp is non-CircuitPython electronics). (Options were: (a) per-experiment; (b) themed families; (c) mixed; (d) families spanning beyond CircuitPython.) Hedge retained: start finest-grain; coarsen later via the `_INDEX.md` family column without a structural change (R-1).
- **D2 (OQ2) Promotion autonomy. — DECIDED = (b).** Auto-promote cross-project patterns at the 2-project trigger; sign-off required only for `[universal]`. (Options were: (a) propose-only; (b) auto + sign-off for universal; (c) full autonomy.) Reversible via the M6 demotion path (mark `disputed`, log in CHANGELOG) (R-4).
- **D3 (OQ3) Scope-tagging of pre-existing entries. — DECIDED** (recommendation accepted, governed by the principle below + R-7 fix). Policy for untagged entries at move time. **Governing principle (the load-bearing part — not the default rule): never *guess* an association into a deep, specific bucket.** A wrong-bucket placement is asymmetrically costly — a mis-buried entry becomes invisible from where it should be retrieved and silently rots (a cross-project insight tagged `[project:exp14]` is lost to every other project), whereas under-tagging stays findable and is cheap to correct later. So when scope is uncertain: keep the entry at the *shallowest correct* scope (broad-but-visible beats narrow-but-wrong) or flag it for review — do not manufacture a precise placement. *Recommendation (from mandate, subordinate to the principle)*: working-style → `[universal]`/`[user]`; **honor any tag already present**; the active-project default applies ONLY to genuinely-untagged technical/conclusions content; route already-`[universal]`/`[cross-experiment]` content to central, not a project (R-7); flag anything ambiguous rather than defaulting it.
- **D4 (Recon-A — mandate Pre-exec-review: scope-model divergence). — DECIDED = keep orthogonal.** Directive-scope (4-tier `[universal]/[user]/[project]/[task]`) and project-content-scope (`family`/`project`) stay as two orthogonal dimensions, since they already coexist (see SoT map in `SESSION_LOG.md`). (Options were: merge with mapping; keep orthogonal; collapse.) Non-destructive — can collapse later if proven redundant, but cannot un-collapse without re-tagging (R-2). (Label note: mandate's reconciliation "Recon-A" — distinct from `_META.md` review step R1.)
- **D5 (Recon-B — mandate Pre-exec-review: stale-tier divergence). — DECIDED = remove (mechanical).** Strip residual `verified`/human-elevation-gate references inside the mandate + anywhere they linger; mechanical now that D4 is set. (Label note: mandate's "Recon-B" — distinct from review step R2.)
- **D6 (DECIDED by Alex) Central memory physical home + reachability.** Alex 2026-06-14: the persona has ONE unified memory in ONE location, reachable from all project workspaces; the whole central tree (not just `universal/`) satisfies C4. **Path: DECIDED = keep the central tree in the CircuitPython workspace's `.cursor/rules/` and symlink it into the other workspaces' `.cursor/rules`** (the in-workspace option; not a neutral user-level root). NOT federated. **Pre-condition (R-6, mandatory)**: before relying on this, smoke-test that the symlink resolves under Cursor's rule loader from each workspace root (esp. Bamboo-Lamp standalone) and survives git ops; if it fails, find another single-location reachability mechanism (do not fall back to federation).
- **D7 (DECIDED by Alex — unified, NOT federated) Bamboo-Lamp integration.** Alex 2026-06-14 overrode the earlier federation recommendation. Bamboo-Lamp's **high-level memory** (digest, rough status, generalizable + boundary learnings, modus-operandi/style feedback) migrates into the central persona memory; Bamboo-Lamp's **technical artifacts** (`AI-Notes.md` technical detail, sketches, diagrams under `Bamboo-Lamp/diagrams/`) stay in the Bamboo-Lamp repo as artifacts, **linked** from central. The existing `/Users/alex/Projects/Family/Bamboo-Lamp/memory/` high-level content is migrated to central and replaced with pointers (Phase 4). Satisfies the unified-persona requirement (§1, C4).
- **D8 Concept-graph retrieval layer vs. mandate's flat per-project files. — DECIDED = (c) graduated** (`concepts/_INDEX.md` + `_RELATIONS.md` from day one; one `concepts/<domain>.md` per evidenced domain; split to per-concept files only when a domain grows unwieldy). Carries the R-9 resolution: the prose→concept reshaping of the two existing `TECHNICAL.md` sections is an **explicitly Alex-approved exception** to the mandate's "no content rewrite" anti-goal, scoped to those sections only; and the R-8 resolution: no-loss (S2) is verified by **claims-coverage** (every claim/finding in the snapshot reappears by content post-reset), not a line/entry diff. The mandate's target architecture stores domain knowledge in per-project monolithic `TECHNICAL.md` / `CONCLUSIONS.md`. The design rubric (`microcontroller-multi-project-memory-guidelines.md` §3.1) **explicitly replaces** the "read the big file and scan" model with a **concept-graph**: an always-read `concepts/_INDEX.md`, a typed `concepts/_RELATIONS.md` edge list (lateral concept↔concept traversal — e.g. fonts↔display, memory-management↔led-driving), and one `concepts/<domain>.md` per evidenced domain. Retrieval-efficiency outranks parsimony (guidelines §0/§9), and the concept-graph is what serves it; the mandate predates the guidelines (guidelines Cross-refs flags exactly this reconciliation). **This is a deviation from the mandate, so it is Alex's call, not the plan's.** Options: **(a)** mandate-as-written — flat per-project `TECHNICAL`/`CONCLUSIONS`, accept that lateral concept-traversal is unsupported; **(b)** full concept-graph immediately; **(c)** *graduated* — keep `concepts/_INDEX.md` + `_RELATIONS.md` from day one (cheap retrieval skeleton), start each evidenced domain as one `concepts/<domain>.md` section-per-concept file, split to per-concept files only when a domain grows unwieldy (guidelines §3.1 graduation). *Recommendation*: **(c)** — gives one-hop topic→concept retrieval + lateral edges now, at minimal scaffolding, consistent with C7. **Relation to `crossref/`**: `_RELATIONS.md` is the concept↔concept axis; `crossref/BY_TOPIC|BY_PATTERN` is the cross-*project* axis (which projects touch a topic). They are orthogonal — neither replaces the other; if (a) is chosen, only the cross-project axis exists. **Alex's unification directive (2026-06-14) reinforces (b)/(c) over (a)**: boundary concepts (between project-specific and general) must live in *central* memory, not project folders — i.e. there must be a central concept home. `concepts/` is exactly that home, so the unified-memory requirement raises the value of the concept-graph layer.

## 3a. Adaptations of the mandate-template (assessed during execution)

The mandate is a prior-execution template, not a rigid spec (see header). These are the adaptations to the persona's current state that the template warrants; **low-stakes** ones are made transparently and logged, **high-stakes / structural** ones escalate to Alex:

- **DV1 — `universal/` holds more than `WORKING_STYLE.md`.** Mandate step 4 says move only `WORKING_STYLE.md`. This plan also moves `CODING_PRINCIPLES.md` + `MONITORING.md` + `CHANGELOG.md` into `universal/`. Reason: those files were created 2026-04-21, *after* the mandate was declared (2026-04-17) — the template predates them; they are behavioral/structural memory and belong with `WORKING_STYLE.md`. **Stakes: low** (behavioral, shared by design) → transparent adaptation, logged in `CHANGELOG`.
- **DV2 — `reference/` stays in place.** The mandate does not move `reference/`; an earlier draft (v0.0 Phase 2) did. Moving it would orphan ~15+ `reference/<file>` path references across `00`/`01`/`03`-`.mdc` + mandate + `COLLABORATOR_GUIDE.md` for zero retrieval gain. Keep `reference/` at `.cursor/rules/reference/`. **Stakes: low** (keeping = no change; moving = high dead-path risk) → transparent adaptation.
- **DV3 — Concept-graph layer (D8). — ESCALATION RESOLVED.** Adopting guidelines §3.1's concept-graph is a structural divergence from the mandate's flat per-project files. **Stakes: high (structural)** → was escalated; **Alex approved (c) graduated on 2026-06-14**, with the prose→concept reshaping scoped to the two existing `TECHNICAL.md` sections as an approved anti-goal exception (R-9).

## 4. Approach (PROVISIONAL — labelled; expected to evolve)

Treat the mandate's 12-step Warm Reset Protocol as an **adaptable template** (not a fixed script): work through it, decomposed into phases sized to the learning rate (§5), while assessing template-fit against the persona's current state (Phase 0) and adapting where warranted. The phase↔mandate-step / M-mechanism mapping is given inline per phase (phases reference M1/M2/M3/M5/M6, DV1–3, and the C-constraints they realize) rather than as a separate table — step sequences stay open per the flexible-plan posture. Decisions (§3) front-loaded into Phase 0 so execution phases are mostly mechanical. Each phase ends with a checkpoint (Observe→Evaluate→Revise→Continue) and a criteria-revision gate.

## 5. Phases (PROVISIONAL — sequence/shape may change; only Phase 0's first action is firm)

- **Phase 0 — Decisions, reconciliations & template-fit assessment.** Assess the mandate-template against the persona's current memory state (the content inventory, the evolved structure since the mandate was declared) and identify warranted adaptations (low-stakes → make transparently + log; high-stakes/structural → escalate, e.g. DV3/D8). Resolve D1–D8 with Alex. Update the mandate's §Pre-execution-review divergences accordingly. *Exit ramp*: do not enter Phase 1 until D1–D5 + D8 are resolved (D8 sets whether the concept-graph layer exists, which shapes Phase 3; D6/D7 may resolve in parallel but block Phase 4).
- **Phase 0.5 — Pre-flight (mandate checklist).** Before any mutation, run the mandate's `§ Pre-warm-reset checklist for the agent` in full: read failure-modes **F1 (Compaction Catastrophe), F8 (Purpose Conflation), F10 (Duplicated Data Drift)** in `reference/06-failure-modes.md`; re-read `00`/`01`/`02`-`.mdc` + `WORKING_STYLE.md` current state; confirm D1–D8 decisions are recorded; state the planned changes back to Alex; await explicit "go ahead". This is the bridge from Phase 0 (decisions) to Phase 1 (first mutation); it is part of the execution gate, not optional.
- **Phase 1 — Snapshot & scaffold.** C2 snapshot; create `universal/`, `projects/`, `crossref/`; create `universal/CHANGELOG.md` (or move existing) with the warm-reset entry as first line.
- **Phase 2 — Move universal.** Move `WORKING_STYLE.md`, `CODING_PRINCIPLES.md`, `MONITORING.md`, `CHANGELOG.md` into `universal/` (DV1). **Leave `reference/` in place** (DV2 — do not move). Create empty `universal/PATTERNS.md`.
- **Phase 3 — CircuitPython projects (seed by evidence, C7; central = digest + links, C8).** Create `projects/_INDEX.md` (roster + path globs). Create a `projects/<slug>/` digest **only where content exists**: `circuitpython-exp14-display/` (full digest) ; `circuitpython-exp15-*/` as a **minimal stub** (active project, ~1 session entry). Do **not** pre-create folders for exp09/11/13 (residue-only — `_INDEX.md` row only if/when content surfaces). Each central project digest holds a **high-level digest** (what was done + rough status + resumption point) + **entry points** that *link* into the project folder's technical artifacts (e.g. `2026-04_Exp14_*/CONTEXT_HANDOFF.md`, `lib/` sources, `working-docs/`) — **not** a copy of the project's full technical detail (C8). **Hoist generalizable + boundary content to central** (`concepts/` per D8, `universal/` for behavioral) rather than into the per-project digest — the current `TECHNICAL.md` content (CircuitPython memory-management, fonts) and the `CONCLUSIONS` finding are boundary/general and belong in central concepts, not buried per-project (risk R-7). Pure-execution detail (code maps, pin assignments) stays as project-folder artifacts, linked. Move + tag the high-level slices per D3; `CONTEXT.md` *references* the existing handoff doc (`CONTEXT_HANDOFF.md`) rather than copying it (one canonical home per fact, guidelines §9, avoids F10 drift). **If D8 = (b)/(c)**: create `concepts/_INDEX.md` + `_RELATIONS.md` + one `concepts/<domain>.md` per evidenced domain (today: `circuitpython-runtime`, `fonts`, `display`) — no empty domains.
- **Phase 4 — Bamboo-Lamp integration (D7 = unified).** Migrate Bamboo-Lamp's **high-level** memory from `/Users/alex/Projects/Family/Bamboo-Lamp/memory/` into central: create `projects/bamboo-lamp/` digest (status + resumption + entry points linking into `Bamboo-Lamp/` artifacts — `AI-Notes.md`, `diagrams/`, etc.); hoist its generalizable/boundary learnings to `concepts/`/`universal/`. Leave Bamboo-Lamp **technical artifacts in the repo** (linked, not moved). Replace the emptied `Bamboo-Lamp/memory/` high-level files with a pointer/README to central (so a session opened there finds its way back). Ensure `projects/_INDEX.md` routes `Bamboo-Lamp/**` to `projects/bamboo-lamp/`. **Cross-contamination note**: until execution, the *current* routing split still holds (do not write Bamboo-Lamp content into CircuitPython memory now) — unification happens only at warm-reset execution.
- **Phase 5 — crossref.** Create `crossref/BY_TOPIC.md` + `BY_PATTERN.md` with format headers. Header-only at reset is **expected** (cross-project patterns need 2+ projects with content; only exp14 is substantial today) — this is the retrieval skeleton kept from day one (guidelines §3.1), not a C7 violation; first entries appear when a second project accrues content.
- **Phase 6 — Rule-file updates.** `00-memory-system.mdc`: (i) re-point paths **and** (ii) replace § Active Retrieval's *flat* session-start read order with the M5 **project-scoped** order — always read `universal/*` + `projects/_INDEX.md`; read the active project's `CONTEXT`/`SESSION_LOG`(last 1–2)/`TECHNICAL`/`CONCLUSIONS` only (+ `concepts/_INDEX.md` if D8≠(a)); other projects only via `crossref/` — or make § Active Retrieval explicitly defer to `04-multi-project.mdc § Attention scoping` as the single source of truth (no two files prescribing different read orders — F8/F10 hazard). **Update `01-interaction-style.mdc`** (it references `CONCLUSIONS.md`@36 and `WORKING_STYLE.md`@66/84/93 — omitted in v0.0); replace `02-domain-structure.mdc` Active-Experiment-Detection with the M1 active-project identifier; handle `03-memory-update-triggers.mdc` (split vs inline per mandate divergence note — default split into new `04-multi-project.mdc`); add `04-multi-project.mdc` (scope tagging M2, promotion ladder M3, attention scoping M5, demotion M6, **and the placement gate** — guidelines §4's deterministic-home decision procedure that routes every new finding to exactly one destination; this is what S7-3 tests. The gate is parameterized by D8: its "domain knowledge" branch routes to `concepts/<domain>` if D8≠(a), else to per-project `TECHNICAL.md`). Also re-point `COLLABORATOR_GUIDE.md` and the mandate's own path references (Phase 7 covers COLLABORATOR_GUIDE).
- **Phase 7 — Teardown & propagation.** Remove emptied top-level files; verify no orphaned path refs (grep); update `COLLABORATOR_GUIDE.md`.
- **Phase 8 — Verification.** Run §7 success criteria; log completion in `universal/CHANGELOG.md` with `status: warm-reset-completed`.

## 6. Authority handoffs (FIXED)

- **Autonomous**: mechanical moves, scaffolding, path re-pointing, tagging per the agreed D3 policy; **low-stakes template adaptations made transparently + logged (§3a DV1, DV2)**.
- **Escalate**: D1–D8; **high-stakes / structural template adaptations (§3a DV3)**; any content that resists clean tagging (ambiguous scope); any discovered mandate inconsistency.
- **Scope discipline**: if execution reveals the move is bigger/messier than planned (e.g. entries that don't fit one project), STOP and surface before expanding scope.

## 7. Success criteria (INITIAL — revisable at every checkpoint)

- **S1** Snapshot exists and is complete (C2).
- **S2** Content-preservation diff: entry set identical pre/post (C1).
- **S3** No dangling **qualified** path references after the move (C3). Grep ALL rule files (`00`,`01`,`02`,`03`, new `04`) + `COLLABORATOR_GUIDE.md` + the mandate for both **qualified** `memory/<file>` paths (these break when files move) and **bare-name** mentions (`WORKING_STYLE.md` etc. — these still resolve conceptually but update for accuracy). Acceptance = zero dangling qualified paths; bare-name mentions reviewed; **and exactly one file prescribes the session-start read order** (no `00` vs `04` conflict — F8/F10 hazard).
- **S4** Cold-AI session-start simulation succeeds for: CircuitPython exp14, exp15, Bamboo-Lamp (each retrieves its own digest + entry points; none can write another's) (C5, target test).
- **S5** The single central memory loads from *every* project workspace — exp14, exp15, Bamboo-Lamp (one location, reachable; symlinks/path resolve) (C4).
- **S8** Central self-sufficiency: each central high-level entry is usable without opening a project folder; project-folder links resolve and lead to the technical detail (further-reading, not load-bearing) (C8). Spot-check the exp14 + Bamboo-Lamp digests.
- **S6** `universal/CHANGELOG.md` logs the reset; **rollback stated as an exact restore command** (e.g. remove the new tree + `cp -R memory-pre-warm-reset-<ts>/ memory/`), not merely "documented"; snapshot existence is S1.
- **S7** Retrieval acceptance tests pass (guidelines §8 — operationalizes the §1 outcome test, the rubric's #1 priority): (1) **domain one-hop** — a real domain question (e.g. "RP2040 GC heap doubling behaviour?") resolves via `_INDEX`→domain→concept with no speculative search; (2) **lateral relation** — a related-concept query surfaces the linked concept via `_RELATIONS` (applies only if D8≠(a)); (3) **cross-project pollination** — a pattern lands at exactly one deterministic placement-gate home. Run whichever apply given the D8 choice.

## 8. Exit ramps (FIXED)

Pause + surface if: a §3 decision blocks progress; C1 (no-loss) cannot be guaranteed for some entry; the mandate proves internally inconsistent; or the task drifts from "execute the mandate" toward "redesign the mandate".

## 9. Termination & checkpoints

Each phase close = a checkpoint running the full closed-loop cycle: **Observe** (what did this phase reveal?) → **Evaluate** (does it change targets/constraints/known-unknowns/criteria/approach?) → **Revise** (state a specific change *or* explicit "no change, continuing") → **Continue**. Plus a **criteria-revision gate**: re-ask "are S1–S7 still the right criteria?" *and* "did this phase surface a new criterion?" — if so, record it with dated provenance and escalate its adoption per §6 (do not silently absorb). Plan-execution terminates at Phase 8 success; the *planning loop* terminates per `_META.md` diminishing-returns rule.

## 10. Status — all decisions resolved; what remains before execution

**D1–D8 are all DECIDED (Alex, 2026-06-14).** The load-bearing set {D1 granularity = per-experiment+family, D4 scope-model = orthogonal, D8 concept-graph = (c) graduated} is settled, as are D2/D3/D5/D6/D7 and the risk-register fixes R-7/R-8/R-9. **Remaining before execution — exactly two items:**
1. **R-6 reachability smoke-test** (execution-time pre-check, Phase 0.5/4): confirm the `.cursor/rules` symlink resolves under Cursor's rule loader from each workspace root and survives git ops.
2. **The hard gate**: Alex says the exact phrase **"warm reset"** + go-ahead. Until then, no file mutation; the current memory split stands.

At every checkpoint still re-weight the load-bearing trio first (their blast radius is largest if execution surfaces a reason to revisit).
